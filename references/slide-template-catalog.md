*[日本語](slide-template-catalog.ja.md)*

# Slide Template Catalog (All 108 Types)

An image catalog generated by actually rendering each template registered under
`slide-templates/` one page at a time. Its purpose is to **let you look at the output and choose which template to use for a given slide**.
For how to write the schema, see [template-schema.md](template-schema.md);
for generic page patterns (skeletons and figure composition), see
[slide-pattern-catalog.md](slide-pattern-catalog.md).

Each template's **figures** line lists the `type` names of the drawing components used on that page.
The **Inputs** table lists the slots a template takes: name, type, whether it is required, and its limits.

To name a template explicitly, write `$template` on a slide in a deck spec
and that template expands into one slide. The keys under `data` are the slot
names from the template's **Inputs** table:

```json
{
  "slides": [
    {
      "$template": "swot-analysis",
      "data": {
        "title": "Our strategic position",
        "quadrants": ["Strength: …", "Weakness: …", "Opportunity: …", "Threat: …"],
        "insight": "…",
        "source": "Board meeting, March 2026"
      },
      "notes": "Speaker notes (optional)"
    }
  ]
}
```

Keys other than `$template` / `data` / `density` / `lang` are merged over the
rendered slide, so a spec can attach `notes` or retarget `layout`. Density comes
from the slide, then the spec, then the template default. Expansion runs before
validation, generation and `--into`, so `--dry-run --strict` checks the inputs
before anything is created.

The words a template prints for itself — table headers, axis ends — follow `lang`
and come from `slide-templates/i18n/<lang>.json`. What you write under `data` is
never translated. A key with no translation prints in the default language, so a
gap is visible rather than blank.

To render a single slide on its own, use `render_slide_template.py`:

```bash
.venv/bin/python scripts/render_slide_template.py \
    --template swot-analysis --data my-swot.json --out out/swot.json
```

```bash
# Rebuild this catalog (same steps when adding a template)
for pack in marketing-analysis b2b-sales scalar-ae planning calendar analysis read-alone business-plan nexus hearing case-studies proposal marketing partner; do
  .venv/bin/python scripts/build_slide_template_catalog.py \
      --pack $pack --out out/template-catalog/$pack.json
done
# Merge every spec's slides into one deck, generate with build_deck.py,
# then place the fetch_thumbnails.py PNGs at references/images/slide-templates/<id>.png
.venv/bin/python scripts/build_template_catalog_doc.py
```

> `architecture-exhibit`, `ui-mock-flow` and `ui-mock-detail` have no catalog
> image: they embed screenshots that only a real nexus-architect run produces.

| Pack | Count | What this section is for |
|---|---|---|
| [Marketing Analysis Pack](#marketing-analysis) | 8 types | Pages for analyzing markets, customers, and campaign effectiveness |
| [B2B Sales Pack](#b2b-sales) | 8 types | Pages that visualize deal stakeholder structure and discovery (issue exploration) |
| [Scalar AE Pack](#scalar-ae) | 10 types | Standard pages Scalar account executives use for deal reviews and activity plans |
| [Planning Pack](#planning) | 3 types | Pages that show time-based plans |
| [Calendar Pack](#calendar) | 9 types | Pages for date-based schedules and tasks |
| [Current-State Analysis Pack](#analysis) | 10 types | Pages built from consulting current-state analysis and issue-identification frameworks |
| [Read-Alone Pack](#read-alone) | 8 types | Pages of high-density slides that can be read to completion on a single page |
| [Business Plan Pack](#business-plan) | 8 types | Pages covering what an approver looks at first in a business plan or investment proposal: revenue, investment, risk, and the team |
| [Nexus Report Pack](#nexus) | 21 types | Pages that turn an architecture analysis run into slides |
| [Hearing Pack](#hearing) | 6 types | Pages that ask the customer or an audience for what is still missing |
| [Case Study Pack](#case-studies) | 3 types | Pages that carry published customer cases into a deck |
| [Proposal Pack](#proposal) | 6 types | The sections of a problem-solving proposal that had no reusable page |
| [Marketing Pack](#marketing) | 6 types | Pages aimed at people who are not yet in a deal |
| [Partner Pack](#partner) | 2 types | Partner-facing value and the joint-proposal policy |

<a id="marketing-analysis"></a>

## Marketing Analysis Pack (`marketing-analysis`)

Pages for analyzing markets, customers, and campaign effectiveness. Formats range from strategy (SWOT / 3C / TAM-SAM-SOM / positioning) through customer understanding (RFM / cohort), behavior (funnel), to effect verification (A/B testing) — one page per format.

### SWOT Analysis (`swot-analysis`)

![SWOT Analysis](images/slide-templates/swot-analysis.png)

Organizes internal and external environment into four positive/negative quadrants and shows priority actions

**Answers**: What are our strategic strengths, weaknesses, opportunities, and threats?

**figures**: `governing_message`, `matrix`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `quadrants` | `string[]` | ✔ | 4 items; ≤ 80 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Distinguish between facts and interpretations
- Do not assert causal relationships based on SWOT wording alone

### 3C Analysis (`three-c-analysis`)

![3C Analysis](images/slide-templates/three-c-analysis.png)

Shows the strategic focus arising from the overlap of Customer, Competitor, and Company

**Answers**: Where is the strategic territory where customer value and competitive advantage overlap?

**figures**: `governing_message`, `venn`, `table`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `labels` | `string[]` | ✔ | 3 items; ≤ 40 chars |
| `center` | `string` | ✔ | ≤ 36 chars |
| `details` | `string[][]` | ✔ | 3 items; ≤ 60 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

### TAM / SAM / SOM (`market-sizing`)

![TAM / SAM / SOM](images/slide-templates/market-sizing.png)

Shows the total market down to the obtainable market as nested circles

**Answers**: How large is the market, and what is the realistic obtainable range?

**figures**: `governing_message`, `nested_circles`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `rings` | `string[][]` | ✔ | 3–4 items; ≤ 40 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

### Positioning Map (`positioning-map`)

![Positioning Map](images/slide-templates/positioning-map.png)

Shows the positional relationship between us and competitors on two axes

**Answers**: Are we delivering value that differs from competitors?

**figures**: `governing_message`, `posmap`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `points` | `array` (string≤24, number≤1, number≤1) | ✔ | 2–8 items |
| `xAxis` | `string[]` | ✔ | 2 items; ≤ 24 chars |
| `yAxis` | `string[]` | ✔ | 2 items; ≤ 24 chars |
| `highlight` | `string` | ✔ | ≤ 20 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

### RFM Segments (`rfm-segments`)

![RFM Segments](images/slide-templates/rfm-segments.png)

Compares customer segments and measures based on purchase recency, frequency, and monetary value

**Answers**: Who are the loyal, developing, and dormant customers, and which measures should be applied to each?

**figures**: `governing_message`, `table`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `rows` | `string[][]` | ✔ | 3–7 items; ≤ 44 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

### Cohort Retention (`cohort-retention`)

![Cohort Retention](images/slide-templates/cohort-retention.png)

Compares retention rates by acquisition cohort across periods

**Answers**: Is the retention rate of newly acquired customers improving?

**figures**: `governing_message`, `table`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `headers` | `string[]` | ✔ | 4–10 items; ≤ 16 chars |
| `rows` | `string[][]` | ✔ | 3–10 items; ≤ 20 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Do not naively compare bottom-right cells that cover different observation periods
- Note cohorts with small sample sizes

### Conversion Funnel (`conversion-funnel`)

![Conversion Funnel](images/slide-templates/conversion-funnel.png)

Shows counts at each stage of the purchase process and the biggest drop-off point

**Answers**: Which stage's drop-off should be prioritized for improvement?

**figures**: `governing_message`, `funnel`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `stages` | `string[][]` | ✔ | 3–6 items; ≤ 32 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

### A/B Test Results (`experiment-result`)

![A/B Test Results](images/slide-templates/experiment-result.png)

Shows control and treatment group results, sample sizes, the difference, and the verdict on a single page

**Answers**: How much did the key KPI change as a result of the measure?

**figures**: `governing_message`, `vbars_grouped`, `table`, `so_what`, `source_note`  
**Inference level**: Causal (asserting causes)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `categories` | `string[]` | ✔ | 1–4 items; ≤ 20 chars |
| `series` | `array` (string≤20, number[]) | ✔ | 2 items |
| `resultRows` | `string[][]` | ✔ | 3–5 items; ≤ 48 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 180 chars |

Guardrails:

- Do not conclude that a non-significant result means zero effect
- State the pre-defined primary KPI and analysis period

<a id="b2b-sales"></a>

## B2B Sales Pack (`b2b-sales`)

Pages that visualize a deal's stakeholder structure and discovery (issue exploration). Each page shares, on one slide, who influences the decision and what has not yet been learned.

### Influence Map (`influence-map`)

![Influence Map](images/slide-templates/influence-map.png)

Places buying-committee members on two axes — influence and stance — to show whom to move to win the deal

**Answers**: Whom do we need to move to close this deal, and is influence skewed toward the opposing side?

**figures**: `governing_message`, `posmap`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `stakeholders` | `array` (string≤6, number≤1, number≤1) | ✔ | 3–9 items |
| `xAxis` | `string[]` | ✔ | 2 items; ≤ 24 chars |
| `yAxis` | `string[]` | ✔ | 2 items; ≤ 24 chars |
| `champion` | `string` | ✔ | ≤ 6 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Base positions on statements confirmed in meetings. Do not place sales' own hopes as if they were the stakeholder's stance
- Do not place uncontacted stakeholders as "neutral." Put unknowns into discovery-gaps instead
- Base influence on whether the person actually mattered in past approvals, not on job title
- Keep bubble labels to 6 characters or fewer. Put job-title and name details in buying-committee instead
- Bubbles are 0.95in in diameter. If placed too close together, labels get hidden (the --dry-run figure audit stops on "Text is hidden behind a shape drawn later"). Merge stakeholders that appear at the same position, or split them out into buying-committee

### Buying Committee (`buying-committee`)

![Buying Committee](images/slide-templates/buying-committee.png)

Lists each buying-committee member's role, influence, stance, and contact status

**Answers**: Who is involved in the purchase, and how much access have we gained so far?

**figures**: `governing_message`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `members` | `string[][]` | ✔ | 3–7 items; ≤ 22 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Write the buying role as the function this person serves in this deal (approver, champion, evaluator, user, gatekeeper, opponent), not their job title
- Write contact status based on actual meetings held. Do not count a mere business-card exchange as "contacted"

### Decision Structure (`decision-structure`)

![Decision Structure](images/slide-templates/decision-structure.png)

Shows, as a tree, which path an approval request follows and where it could stall

**Answers**: In what order does approval move up, and where might it stop?

**figures**: `governing_message`, `orgchart`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `tree` | `tuple` (string≤30, array) | ✔ | — |
| `insight` | `string` | ✔ | ≤ 110 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Draw this deal's approval route, not the org chart. Do not include departments that are not involved
- If the route changes depending on the amount, state in the source line that this is the route for the assumed amount
- Depth of 4 or more, or more than 8 leaves, causes text to become crushed. Split by department

### Discovery Map (`discovery-map`)

![Discovery Map](images/slide-templates/discovery-map.png)

Lists the items to confirm during the deal and color-codes each as confirmed, hypothesis, or unconfirmed

**Answers**: What do we know in this deal, and what is still just a hypothesis?

**figures**: `governing_message`, `ghost`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `items` | `array` (integer≤99, string≤16, string≤34, string≤9) | ✔ | 4–8 items |
| `insight` | `string` | ✔ | ≤ 110 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- State is one of three values: confirmed (verified by the customer's statement or a document), wip (partially confirmed), or missing (unconfirmed)
- Do not mark sales' own guesses as confirmed. Anything whose corroborating source cannot be named is wip or lower
- Do not move on to drafting the proposal while red (missing) items remain

### Pain Chain (`pain-chain`)

![Pain Chain](images/slide-templates/pain-chain.png)

Traces how a frontline problem cascades into departmental and executive metrics

**Answers**: Which executive-level number is the frontline issue actually affecting?

**figures**: `governing_message`, `lead_in`, `flow`, `table`, `source_note`  
**Inference level**: Causal (asserting causes)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `chain` | `string[]` | ✔ | 3–5 items; ≤ 18 chars |
| `evidence` | `string[][]` | ✔ | 3–5 items; ≤ 40 chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- A chain link is a causal claim. Do not connect two stages with an arrow unless each stage has supporting evidence
- Describe the impact on executive metrics using the customer's own numbers. If it's our own estimate, label it clearly as an estimate
- Do not present correlation as causation

### Unconfirmed Items and Next Steps (`discovery-gaps`)

![Unconfirmed Items and Next Steps](images/slide-templates/discovery-gaps.png)

Breaks down the open questions that remain blank into who will confirm them and by when

**Answers**: Whom do we need to ask what, next, to move the deal forward?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `gaps` | `string[][]` | ✔ | 3–6 items; ≤ 40 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Do not ask about an item if you cannot state why it matters. What decides success is the order of questions, not the quantity
- Do not leave a row without a person to confirm with and a deadline
- Keep this aligned one-to-one with the missing / wip items in discovery-map

### Influence Map (Org Structure) (`influence-map-org`)

![Influence Map (Org Structure)](images/slide-templates/influence-map-org.png)

Arranges buying-committee members by organizational connections, showing role, influence, stance, and meeting status

**Answers**: Who reports to whom, and where is influence and support concentrated?

**figures**: `governing_message`, `influence_graph`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `people` | `array` | ✔ | 2–9 items |
| `links` | `array` | — | ≤ 4 items; default `[]` |
| `more` | `string` | — | ≤ 60 chars; default `""` |
| `insight` | `string` | ✔ | ≤ 58 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The contents of people / links are validated by scripts/account_graph.py. Duplicate ids, dangling reportsTo references, and cycles are caught there, so always pass data that has gone through account_graph.load()
- If there are more than 9 people, thin them out with account_graph.extract() and write "see the draw.io version for the other N people" in more. Do not cram everyone onto one slide
- Include unmet stakeholders with met: false (drawn as a dashed line). Not having met someone is itself information
- Base stance on statements made in meetings. Do not write sales' own hopes into stance

### Discovery Map (Hierarchy) (`discovery-map-tree`)

![Discovery Map (Hierarchy)](images/slide-templates/discovery-map-tree.png)

Connects Goal, Strategy, and Tactics by support relationships, showing what supports what

**Answers**: What supports the customer's goal, and where do we make a difference?

**figures**: `governing_message`, `outcome_tree`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `nodes` | `array` | ✔ | 2–9 items |
| `edges` | `array` | — | ≤ 20 items; default `[]` |
| `more` | `string` | — | ≤ 60 chars; default `""` |
| `insight` | `string` | ✔ | ≤ 58 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The contents of nodes / edges are validated by scripts/account_graph.py. Cycles and downward-pointing edges are caught there, so always pass data that has gone through account_graph.load()
- The tier is determined by graph depth, not by a tier field. A lower-level goal that supports a higher-level goal is placed one level below it
- If there are more than 9 nodes, thin them out with account_graph.extract() and write "see the draw.io version for the other N items" in more
- Do not place a goal in Goal that the customer never stated. Treat guesses as wip items in the discovery map instead

<a id="scalar-ae"></a>

## Scalar AE Pack (`scalar-ae`)

Standard pages that Scalar account executives use for deal reviews and activity plans. Designed so the contents of account.json (the deal ledger) can be poured in directly. The only stable pack.

### Deal Snapshot (`account-snapshot`)

![Deal Snapshot](images/slide-templates/account-snapshot.png)

Shows, on one page, what stage this deal is in now, the amount, and when it will close

**Answers**: Where does this deal stand right now, for how much, and when will it close?

**figures**: `governing_message`, `cards`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `headline` | `array` (string≤10, string≤24) | ✔ | 4 items |
| `rows` | `string[][]` | ✔ | 3–4 items; ≤ 46 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- forecast is one of four values: Pipeline / Best / Commit / Closed. Demote a Commit to Best if you cannot state its basis
- Write an amount only if you can state how it was calculated. If you cannot, write "undetermined"
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Stage-Gate Criteria (`phase-gate`)

![Stage-Gate Criteria](images/slide-templates/phase-gate.png)

Lists the current stage's exit criteria and judges each by whether customer-side evidence has been obtained

**Answers**: What customer-side evidence shows that the current stage's exit criteria are met?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `stage` | `string` | ✔ | ≤ 60 chars |
| `gates` | `string[][]` | ✔ | 3–6 items; ≤ 40 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- State is one of three values: met / partial / unmet. "We explained it" or "we sent the proposal" does not count as grounds for met
- Evidence must come from the customer side (statements, documents, approval records). Internal agreement is not evidence
- Map each exit criterion to a gate ID in references/scalar/sales-playbook.md §2
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### BANT Risk (`bant-risk`)

![BANT Risk](images/slide-templates/bant-risk.png)

Identifies which of Budget / Authority / Needs / Timeframe is most likely to stall the deal

**Answers**: Where is the risk in this deal, and is the forecast reasonable?

**figures**: `governing_message`, `cards`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `items` | `array` (string≤16, string≤80) | ✔ | 4 items |
| `insight` | `string` | ✔ | ≤ 110 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Fix the order of the four items as Budget → Authority → Needs → Timeframe
- Open each item with ok / risk / unknown, and write the basis for that judgment in the same cell
- Send any item still marked unknown to action-plan with a person to confirm with and a deadline
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Action Plan (`action-plan`)

![Action Plan](images/slide-templates/action-plan.png)

Breaks each unconfirmed issue down into who, asks whom, by when, and what completion looks like

**Answers**: To move this deal forward, who does what, by when?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `actions` | `string[][]` | ✔ | 3–6 items; ≤ 36 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Do not include a row whose completion criterion cannot be written. "Confirm it" is not a completion criterion
- Do not leave a row without a deadline. If the counterpart is internal, name the meeting body (WPS / Deal Desk)
- Map each row one-to-one to an unconfirmed item in discovery-gaps
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Activity History (`activity-timeline`)

![Activity History](images/slide-templates/activity-timeline.png)

Lists, in chronological order, who we've met and what we learned

**Answers**: How much access have we gained with this customer, and what have we learned?

**figures**: `governing_message`, `timeline`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `milestones` | `array` (string≤10, string≤16) | ✔ | 3–6 items |
| `rows` | `string[][]` | ✔ | 2–4 items; ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Limit the table to the most recent 4 entries. Keep earlier history in the ledger (account.json); do not put it on the slide
- Write only the customer's own statements or documents under "what we learned." Do not write our own impressions
- Do not omit dates, so gaps where we had no contact remain visible
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Visit Plan (`visit-plan`)

![Visit Plan](images/slide-templates/visit-plan.png)

Decides, before the visit, what to get out of it, how to ask, and how to respond

**Answers**: What do we want to get from the customer on this visit, and how will we respond to expected objections?

**figures**: `governing_message`, `lead_in`, `storyline`, `so_what`, `cards`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `context` | `string` | ✔ | ≤ 120 chars |
| `questions` | `string[]` | ✔ | 3–4 items; ≤ 44 chars |
| `ask` | `string` | ✔ | ≤ 90 chars |
| `objections` | `array` (string≤20, string≤52) | ✔ | 2–3 items |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Make the title "the one thing we want to hear from the customer." "Explain X" is not an objective
- Always state in ask which person or department we want introduced next, and why
- Frame questions so the answer isn't just Yes/No (include SPIN's Implication and Need-payoff questions)
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Win Plan (WPS) (`win-plan`)

![Win Plan (WPS)](images/slide-templates/win-plan.png)

A one-page summary of the win strategy and risks used to decide stage advancement and proposal investment in WPS

**Answers**: Why can we win this deal, and how will we neutralize the biggest risk?

**figures**: `governing_message`, `exec_summary`, `table`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `situation` | `string` | ✔ | ≤ 84 chars |
| `complication` | `string` | ✔ | ≤ 84 chars |
| `resolution` | `string` | ✔ | ≤ 84 chars |
| `risks` | `string[][]` | ✔ | 2–3 items; ≤ 46 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Write in resolution why the customer would choose us, not a list of product names
- Pair each risk with a next action we can control. Anything without an actionable next step is capped at Best
- Always accompany this with a verbal request for a continue / hold / withdraw decision
- Internal document. Do not share with customers or partners (it includes individuals' influence, stance, and internal politics)

### Challenge Hypothesis One-Pager (`challenge-hypothesis`)

![Challenge Hypothesis One-Pager](images/slide-templates/challenge-hypothesis.png)

A one-pager that surfaces issues the customer hasn't yet articulated, to open a dialogue

**Answers**: What issue has the customer not yet noticed, and what happens if it's left unaddressed?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: stable

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `steps` | `array` (string≤16, string≤68) | ✔ | 3 items |
| `question` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **For customer presentation**. Do not write about individuals' influence, stance, internal politics, or competitors' weaknesses
- Fix the three cards in the order: groundwork → reframing → evidence (cost of inaction)
- Do not assert as fact. Phrase hypotheses so they read as hypotheses, and always end with a question to the customer
- Do not include figures without a source. Cite the source for any public case study

### License Estimate Summary (`license-estimate`)

![License Estimate Summary](images/slide-templates/license-estimate.png)

The cost page of a proposal: a Scalar license estimate's line items, list price → discount → offered price, and assumptions on one slide

**Answers**: How much does this configuration cost, and what are the breakdown and assumptions?

**figures**: `governing_message`, `lead_in`, `table`, `metric`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `lines` | `string[][]` | ✔ | 2–(print 7 · presentation 4) items; ≤ (print 20 · presentation 14) chars |
| `offerPrice` | `string` | ✔ | ≤ 12 chars |
| `offerLabel` | `string` | — | ≤ 18 chars; default `"Offer price (annual, e…` |
| `pricing` | `string[][]` | ✔ | 3–4 items; ≤ 12 chars |
| `assumptions` | `string[]` | — | ≤ (print 2 · presentation 1) items; ≤ 60 chars; default `[]` |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Transcribe unit prices and amounts from the price-master quotation; never compute them on this slide
- Never show the discount rate. Show only list total, discount amount, and offered price
- Always show the contract term (monthly / annual / 3-year) in the summary table; never state an amount without its term
- Pod counts must match the quotation and the architecture diagram. State the contract-Pod rule (annual contract = maximum concurrently running Pods) as an assumption

### License Pattern Comparison (`license-pattern-compare`)

![License Pattern Comparison](images/slide-templates/license-pattern-compare.png)

Compares quotation patterns (Standard / Premium, different configurations) side by side on configuration, Pod count, and offered price, with an explicit recommendation

**Answers**: Which license pattern fits this customer best, and how much do the options differ?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `patternHeaders` | `string[]` | ✔ | 3–4 items; ≤ 14 chars |
| `rows` | `string[][]` | ✔ | 3–(print 5 · presentation 4) items; ≤ (print 20 · presentation 14) chars |
| `recommendation` | `string` | ✔ | ≤ (print 55 · presentation 30) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Transcribe amounts from the price-master quotation's comparison summary; never show discount rates — compare offered prices
- Keep the row dimensions (configuration, Pods, term, price) aligned across patterns so each row reads straight across
- Write the recommendation from both functional fit and price; never push the cheapest option unconditionally

<a id="planning"></a>

## Planning Pack (`planning`)

Pages that present time-based plans. The three formats — Gantt chart, chronology, and milestone timeline — differ in granularity and use case.

### Gantt Chart (`gantt-schedule`)

![Gantt Chart](images/slide-templates/gantt-schedule.png)

A period-by-task bar chart showing work streams running in parallel and their milestones

**Answers**: When does what run in parallel, and when is the next milestone?

**figures**: `governing_message`, `gantt`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `columns` | `string[]` | ✔ | 3–8 items; ≤ 8 chars |
| `tasks` | `array` (string≤16, number≤8, number≤8, string≤20) | ✔ | 2–8 items |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Start and end are decimals in column units (0 is the left edge of the first column, the column count is the right edge). Values exceeding the column count raise an error at generation time
- A row where start == end is drawn as a milestone (◆). Give a row width if you want it drawn as a bar
- Dependency arrows are not represented. If you need to show fine-grained sequencing, write it as a table instead
- Limit to 8 rows. Bundle fine-grained tasks so each row is one workstream
- Do not mix planned values with actuals. For a planned schedule, state the planning date in source

### Chronology (`chronology`)

![Chronology](images/slide-templates/chronology.png)

A vertical chronology of dates and events showing how we arrived at the current state

**Answers**: What has happened so far, and what sequence of events led to the current state?

**figures**: `governing_message`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `events` | `string[][]` | ✔ | 4–9 items; ≤ 44 chars |
| `insight` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Write only facts in the event column. Keep interpretation and evaluation separate, in "implications"
- Keep the date granularity consistent (all years, or all year-months, not mixed)
- Limit to 9 rows. Bundle earlier history or narrow the period covered
- If a quiet period is meaningful, don't omit the row — write "no change" to show it explicitly

### Milestone Timeline (`milestone-timeline`)

![Milestone Timeline](images/slide-templates/milestone-timeline.png)

Arranges milestones at equal intervals along a single horizontal timeline, showing the next decision point

**Answers**: How are the milestones arranged, and when is the next point we need to act?

**figures**: `governing_message`, `timeline`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `milestones` | `array` (string≤10, string≤16) | ✔ | 3–6 items |
| `insight` | `string` | ✔ | ≤ 160 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Limit to 6 milestones. The timeline is drawn at equal intervals and does not represent duration (use gantt-schedule if you need to show duration)
- State in source that future milestones are planned, not confirmed
- Keep date labels to 10 characters or fewer (e.g., 2026/09), with consistent granularity

<a id="calendar"></a>

## Calendar Pack (`calendar`)

Pages for date-based schedules and tasks. Nine formats — a month calendar (Monday- or Sunday-start), a day-by-day Gantt chart, a daily task list, a weekly timetable, a sprint calendar, a year at a glance, a deadline countdown, a daily heatmap, and a shift roster — with weekend/holiday shading, working-day counts, and automatic splitting of long periods (scripts/calendar_pages.py).

### Month Calendar (`month-calendar`)

![Month Calendar](images/slide-templates/month-calendar.png)

Writes events and multi-day spans onto a one-week-per-row month calendar, showing how busy the month is and where the days off fall

**Answers**: Which days this month have what on them, and where is it busy?

**figures**: `governing_message`, `month_calendar`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `month` | `string` | ✔ | ≤ 7 chars; ≥ 6 chars |
| `weekStart` | `string` | — | ≤ 3 chars; ≥ 3 chars; default `"mon"` |
| `events` | `array` (string≤10, string≤10, string≤20, string≤7, string≤5) | ✔ | ≤ 40 items |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the calendar header
- Each event is [start date, end date, subject, category, time]. Write dates as YYYY-MM-DD, and do not guess the year for dates written without one (such as 9/3)
- With an empty end date the event is written in its day cell as a one-day event; with an end date it is drawn as a span. A span that crosses weeks is split per week, and each continuation is marked as one in the deck's language
- Switch the week start with weekStart (mon / sun). Monday-start shows ISO week numbers on the left; Sunday-start does not
- About two events fit in one cell. The rest collapse into a "+N more" note, so give the details on a daily-agenda page
- Subjects are truncated at about 8 full-width characters. The time is dropped when it does not fit; the subject takes priority
- Holidays are detected automatically only for the years covered by the bundled Cabinet Office CSV. Pass company-specific closures in extraHolidays
- State in source that these are planned events, and as of when

### Day-by-Day Gantt (`daily-gantt`)

![Day-by-Day Gantt](images/slide-templates/daily-gantt.png)

Places task bars on date columns, showing the actual working days across holidays and the milestones at day granularity

**Answers**: How does the work in the period run in parallel day by day, and when does it finish once holidays are counted?

**figures**: `governing_message`, `day_gantt`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `start` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `end` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `rows` | `array` (string≤9, string≤14, string≤6, string≤10, string≤10, number≤1) | ✔ | 1–12 items |
| `today` | `string` | — | ≤ 10 chars; default `""` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the calendar header
- Each row is [kind, name, owner, start date, end date, progress]. kind is group (heading) / task (span) / milestone (◆). A milestone sits in its start-date column with an empty end date and progress 0
- Up to 31 days every day is labeled, up to 60 days only Mondays are, and from 61 days to 26 weeks it switches automatically to one column per week. For longer plans use planning/gantt-schedule (monthly)
- The working-day count (excluding weekends, holidays, and extraHolidays) appears to the right of each bar. Do not put dates on work that has no basis for its duration
- Up to 12 rows. Split larger plans into pages by group with scripts/calendar_pages.py
- Dependency arrows are not drawn. For plans that need to show sequencing, use nexus/roadmap
- Do not mix planned values with actuals. When you include progress rates, state the as-of date in source

### Daily Task List (`daily-agenda`)

![Daily Task List](images/slide-templates/daily-agenda.png)

Lists dates going down with one task per row, showing each day's owner, due time, and status

**Answers**: Who does what by when, day by day?

**figures**: `governing_message`, `day_agenda`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `start` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `end` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `items` | `array` (string≤10, string≤24, string≤6, string≤5, string≤9) | ✔ | 1–11 items |
| `today` | `string` | — | ≤ 10 chars; default `""` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the calendar header
- Each item is [date, content, owner, due, status]. One task per row; the date cells of several tasks on the same day are merged vertically
- Consecutive weekends and holidays with no tasks collapse into one row labeled with the holiday name. A holiday row still counts as a row
- About 11 rows per slide. Split longer periods by week with scripts/calendar_pages.py
- Status is 完了 / 進行中 / 予定 / 遅延 / 中止 (done / doing / todo / late / cancelled also work). It is shown in text, not only by color
- Write a time or deadline in the due column, and leave it empty when undecided (it shows as "—"). Do not fill it in by guessing
- Passing today highlights that day's date cell in red. Align it with the document's creation date, and state the as-of date in source as well

### Weekly Timetable (`weekly-timetable`)

![Weekly Timetable](images/slide-templates/weekly-timetable.png)

Places events on weekday columns and hour rows, showing what happens where at each time of the week

**Answers**: What is on, when and where, this week?

**figures**: `governing_message`, `week_timetable`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `week` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `days` | `integer` | — | 5–7; default `5` |
| `startHour` | `integer` | — | 0–23; default `9` |
| `endHour` | `integer` | — | 1–24; default `18` |
| `events` | `array` (string≤10, string≤5, string≤5, string≤14, string≤10, string≤7) | ✔ | 1–25 items |
| `breaks` | `array` (string≤5, string≤5, string≤6) | — | ≤ 3 items; default `[["12:00", "13:00", "Lu…` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 7 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the header
- Each event is [date, start, end, subject, place, category]. Write times as HH:MM. Put any date of the week in week; it is drawn as a Monday-start week
- For 9:00–18:00 (9 hours), one hour is about 0.37in. Events shorter than 30 minutes cannot be drawn, so move them to a list (daily-agenda) instead of rounding the times
- Up to two events at the same time sit side by side. For a week with three or more overlapping, split pages by room or track
- Breaks (breaks, a 12:00–13:00 lunch break by default) appear as a gray band only on days without events there. Holidays with no events are filled with the holiday name
- To include weekends, set days to 7. Columns get narrower, and a one-hour event shows only its subject
- State in source that these are planned events, and as of when

### Sprint Calendar (`sprint-calendar`)

![Sprint Calendar](images/slide-templates/sprint-calendar.png)

Lays consecutive sprints out in week rows, showing the working days lost to holidays and the planning, review, and release days

**Answers**: When does each sprint run, how many working days does it have, and when are its events?

**figures**: `governing_message`, `sprint_calendar`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `start` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `lengthDays` | `integer` | — | 7–14; default `14` |
| `sprints` | `array` (string≤6, string≤14, boolean) | ✔ | 1–8 items |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the header
- Each sprint is [number, goal, release or not]. start is the Monday of the first sprint; sprints follow one after another every lengthDays (7 or 14)
- Up to 8 weeks per slide (4 two-week sprints, or 8 one-week sprints). Split longer spans with scripts/calendar_pages.py
- Working days are weekdays minus holidays and extraHolidays. The panel also shows how many holidays fell on a weekday, which is grounds for lowering the expected velocity
- Planning is placed on the first working day (marked as moved when the first day is a holiday), and review or release on the last working day, automatically. Do not use this when the actual dates differ
- Goals are one line, up to 14 full-width characters. Phrase them as an outcome that can be checked, not as a task name
- State in source that future sprints are a plan

### Year at a Glance (`year-at-a-glance`)

![Year at a Glance](images/slide-templates/year-at-a-glance.png)

Lays out the fiscal year's 12 months as small month calendars, marking busy periods, closures, and key dates with fills and circles

**Answers**: Where in the fiscal year do the busy periods and key dates fall?

**figures**: `governing_message`, `year_calendar`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `startMonth` | `string` | ✔ | ≤ 7 chars; ≥ 6 chars |
| `marks` | `array` (string≤10, string≤10, string≤12, string≤4) | ✔ | 1–20 items |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 30 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the month headers
- Each mark is [start date, end date, label, kind]. kind is busy (busy period, blue fill) / off (closure, red fill) / key (key date, red circle; leave end date empty)
- Put the first month of the fiscal year (e.g. 2026-04) in startMonth and 12 months are laid out. Dates outside that range are an error
- Dates are 7pt, a density for handouts only. For projection, use month-calendar one month at a time
- The legend lists the labels of each kind. Keep labels short (about 6 full-width characters) and about 3 per kind at most
- To also color company closure dates red, add them to extraHolidays too. An off mark only fills the area
- State in source that these are planned events, and as of when

### Deadline Countdown (`deadline-countdown`)

![Deadline Countdown](images/slide-templates/deadline-countdown.png)

Shows the days left to a deadline in calendar days and working days, and shades the calendar from today to the deadline

**Answers**: How many days (working days) are left to the deadline, and when are the checkpoints on the way?

**figures**: `governing_message`, `deadline_countdown`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `label` | `string` | ✔ | ≤ 12 chars; ≥ 1 chars |
| `deadline` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `today` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `checkpoints` | `array` (string≤10, string≤12) | — | ≤ 3 items; default `[]` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the header
- today is the document's as-of date and is required. Days left are calendar days from today to the deadline; working days are weekdays from today to the day before the deadline, minus holidays and extraHolidays
- The deadline must fall in today's month or the next month. Show deadlines further out with month-calendar or daily-gantt
- Checkpoints are [date, name], up to 3. Use dates between today and the deadline
- Make label a short phrase that says what the deadline is for, such as "本番切替まで" (until the production cutover)
- A document past its as-of date has stale numbers. Update today when distributing, and state the as-of date in source as well

### Daily Heatmap (`activity-heatmap`)

![Daily Heatmap](images/slide-templates/activity-heatmap.png)

Lays out daily values as shaded week × weekday cells, showing weekday and seasonal skew and monthly totals

**Answers**: On which weekdays and in which periods is the volume concentrated?

**figures**: `governing_message`, `calendar_heatmap`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `start` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `end` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `values` | `array` (string≤10, number) | ✔ | 7–371 items |
| `unit` | `string` | — | ≤ 4 chars; default `"items"` |
| `levels` | `integer` | — | 3–7; default `5` |
| `monthly` | `boolean` | — | default `true` |
| `summary` | `boolean` | — | default `true` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 30 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the month headers
- Values are [date, number]. The period is up to 53 weeks. Split longer series into 52-week pages with scripts/calendar_pages.py
- The page handles numbers, so source is required. State the period, the counting definition (what counts as one), the unit, and where the data came from
- Shading is a relative bucket by quantile (levels steps) of this period's values, not an absolute threshold. Do not compare colours with a chart of another period
- Days with no value are white; days with 0 get the lightest colour. Do not mix missing values and zeros (do not fill missing days with 0)
- Low values on holidays and long weekends are a calendar effect, not an anomaly. Read them together with the holiday average card
- The cards below show aggregates only. Put causes or evaluation in the title or body, and do not write correlation as causation

### Shift Roster (`shift-roster`)

![Shift Roster](images/slide-templates/shift-roster.png)

Places a one-character duty code and colour in each person × day cell, showing the daily headcount and understaffed days

**Answers**: Who is on duty each day, and which days are short-staffed?

**figures**: `governing_message`, `shift_roster`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 36 chars |
| `start` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `end` | `string` | ✔ | ≤ 10 chars; ≥ 8 chars |
| `people` | `array` (string≤6, string≤31) | ✔ | 1–12 items |
| `codes` | `array` (string≤1, string≤8, string≤7, boolean) | ✔ | 1–6 items |
| `minStaff` | `integer` | — | 0–12; default `0` |
| `extraHolidays` | `array` (string≤10, string≤10) | — | ≤ 20 items; default `[]` |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The title (the conclusion) is one line, up to 36 full-width characters. A second line overlaps the date headers
- People are [name, schedule]. The schedule is a string of one code character per day of the period (e.g. 日日夜休…), and its length must equal the number of days
- Codes are [character, meaning, colour, counts toward headcount]. Colours are primary / dark / success / danger / info / warning / muted. The legend is added automatically
- One page holds up to 31 days × 12 people. Split longer periods or larger teams by month or by 12 people with scripts/calendar_pages.py
- With minStaff set, days whose total of headcount codes falls below it turn red. State the basis for the required staffing (SLA, staffing plan) in source
- Mind the sharing scope of material that shows real people's names. Use role names or pseudonyms for external audiences and examples
- State in source that this is a plan and as of when the roster applies. Same-day swaps are not reflected

<a id="analysis"></a>

## Current-State Analysis Pack (`analysis`)

Pages built from consulting current-state analysis and issue-identification frameworks. Covers structural decomposition (logic tree / KPI tree), root cause (why-why / fishbone diagram), quantitative narrowing (Pareto chart), gap and process flow, external environment (PEST / Five Forces), and prioritization. Used by the current-state-analysis skill.

### Logic Tree (`logic-tree`)

![Logic Tree](images/slide-templates/logic-tree.png)

Decomposes an issue MECE-style, showing structurally where the problem (or the solution) lies

**Answers**: What elements can this problem be broken down into, and which one matters most?

**figures**: `governing_message`, `mece_tree`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `tree` | `array` | ✔ | 2 items |
| `insight` | `string` | ✔ | ≤ 88 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- tree is nested as [label, [children…]]. Each child is either the same shape or a string. Depth beyond 4, or insufficient column width, raises an error at render time
- Limit to 5 leaves (to preserve the 0.46in node height). Beyond that, split the tree across two slides
- MECE (mutually exclusive, collectively exhaustive) is not enforced by the component itself. Keep one decomposition criterion per level
- Do not mix a Why tree (cause decomposition) and a How tree (solution decomposition) into a single tree
- Write in insight which branch matters most, and the basis for that judgment

### KPI Tree (`kpi-tree`)

![KPI Tree](images/slide-templates/kpi-tree.png)

Decomposes the KGI into its component metrics, showing which metric is driving the gap (or the achievement) against target

**Answers**: Which component metric is producing the gap between target and actual?

**figures**: `governing_message`, `mece_tree`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `tree` | `array` | ✔ | 2 items |
| `insight` | `string` | ✔ | ≤ 88 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- The root is a single KGI. Write node labels as "metric name\nactual value" (or "actual / target" if showing the ratio to target)
- Only use parent-child relationships explainable by arithmetic (Revenue = New + Existing, New = Leads × Conversion rate, …). Do not include elements that can't be explained this way
- Every figure requires a source. State the period and aggregation definition in source
- Limit to 5 leaves (to preserve the 0.46in node height for two-line labels). When there are many metrics, expand only the bottleneck branch and collapse the rest at a higher level
- Write in insight which metric is the primary driver of the gap, quantified (e.g., of the -12% shortfall, -9% comes from the opportunity-conversion rate)

### Why-Why Analysis (`why-why`)

![Why-Why Analysis](images/slide-templates/why-why.png)

Shows the chain produced by repeatedly asking "why" from an observed event down to the root cause

**Answers**: What is the root cause of this event, and where should we act?

**figures**: `governing_message`, `flow`, `so_what`, `source_note`  
**Inference level**: Causal (asserting causes)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `chain` | `string[]` | ✔ | 3–6 items; ≤ 32 chars |
| `insight` | `string` | ✔ | ≤ 88 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- chain[0] is the observed event, and the last entry is the root cause. Don't skip steps — dig one "why" at a time
- Don't stop at "the person in charge was careless" or "low awareness." Dig down to systemic and process-level factors
- Word each step as something verifiable by fact. Mark any step that hasn't been verified with (hypothesis)
- Limit to 6 steps due to width constraints. If the cause branches into multiple paths partway through, use fishbone-diagram instead
- Write in insight the countermeasure (recurrence-prevention action) aimed at the root cause, distinguished from a symptomatic fix

### Fishbone Diagram (`fishbone-diagram`)

![Fishbone Diagram](images/slide-templates/fishbone-diagram.png)

Lists hypothesized causes of a problem by category, to identify where to start verifying

**Answers**: Which category does the cause of this problem fall under, and where should verification start?

**figures**: `governing_message`, `fishbone`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `problem` | `string` | ✔ | ≤ 20 chars |
| `categories` | `array` (string≤8, string[]≤22) | ✔ | 2–6 items |
| `insight` | `string` | ✔ | ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Keep categories to a single consistent basis, such as the 4Ms (Man, Machine, Method, Material) or process names. Do not mix bases
- Limit to 4 causes per category. If there are more, consolidate them or move them to a supplementary document (this diagram is not meant to prove exhaustive coverage)
- What's included here are hypothesized causes. Identify the root cause via why-why or data verification; do not distinguish verified causes with bold or similar formatting (all causes are on equal footing in this diagram)
- State problem as a measurable event ("monthly defect rate above 3%," not "quality is bad")
- Write in insight which cause to verify next, and how

### Pareto Chart (`pareto-analysis`)

![Pareto Chart](images/slide-templates/pareto-analysis.png)

Overlays counts/amounts by cause with cumulative share, showing which cause to tackle first

**Answers**: Do a small number of causes account for most of the total, and where should we start?

**figures**: `governing_message`, `pareto`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `items` | `array` (string≤10, number) | ✔ | 3–10 items |
| `unit` | `string` | — | ≤ 6 chars; default `"items"` |
| `insight` | `string` | ✔ | ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Items are automatically sorted by descending value ("other" is always last). The order you pass them in carries no meaning
- Use only quantities where a sum is meaningful (counts, amounts, time). Do not mix in rates or indices
- State the aggregation period and sample size in source
- The 80% dashed line is a rough guide. When the top causes aren't dominant (the cumulative line is close to straight), "not concentrated" is itself the finding — don't force a conclusion that focuses on the top causes
- If "other" ends up the largest category, your classification is too coarse. Revisit the categories before using this chart

### Gap Analysis (As-Is / To-Be) (`gap-analysis`)

![Gap Analysis (As-Is / To-Be)](images/slide-templates/gap-analysis.png)

Shows the current state (As-Is) and the target state (To-Be) side by side, and defines the gap to be closed

**Answers**: Where does the current state diverge from the target state, and what should we set as the issue to solve?

**figures**: `governing_message`, `before_after`, `cards`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `asis` | `string[]` | ✔ | 2–4 items; ≤ 26 chars |
| `tobe` | `string[]` | ✔ | 2–4 items; ≤ 26 chars |
| `gaps` | `array` (string≤14, string≤34) | ✔ | 1–3 items |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- As-Is must be a verified fact; To-Be must be an agreed-upon goal. If placing an unagreed aspiration into To-Be, mark it explicitly as a hypothesis in the title
- List the same dimensions in the same order under asis and tobe, so the gap reads clearly row by row
- Write gaps as "the issue to be closed," not as "a description of the difference" (what needs to become what state). The issue cards carry this slide's conclusion
- Do not put the details of solutions (How) on this slide. Send prioritization of issues to priority-matrix
- For any numeric target in To-Be, state the basis (benchmark, management target) in source

### Process Flow and Pain Points (`process-painpoints`)

![Process Flow and Pain Points](images/slide-templates/process-painpoints.png)

Shows the current process flow and maps which step has which pain point

**Answers**: How does the current process flow, and which step concentrates the problems?

**figures**: `governing_message`, `flow`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `steps` | `string[]` | ✔ | 3–6 items; ≤ 12 chars |
| `pains` | `string[][]` | ✔ | 1–5 items; ≤ 30 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- steps must be the current (As-Is) flow. Do not mix in an ideal flow or the post-improvement state
- Each row of pains is [step, pain point, impact]. Match the step name exactly to how it's written in steps
- List only pain points verified by observation or interviews as facts. State impact in measurable terms (effort, time, money) and record the source in source
- Limit to 5 rows. For a detailed flow with more than 6 steps, put it in a separate drawio-diagrams sheet and bundle the main steps here
- Do not put solutions on this slide (send issue structuring to logic-tree, prioritization to priority-matrix)

### PEST Analysis (`pest-analysis`)

![PEST Analysis](images/slide-templates/pest-analysis.png)

Lists macro-environment factors across Political, Economic, Social, and Technological dimensions, organizing external impacts on the business

**Answers**: What external factors are shaping our business environment?

**figures**: `governing_message`, `comparison`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `political` | `string[]` | ✔ | 1–4 items; ≤ 24 chars |
| `economic` | `string[]` | ✔ | 1–4 items; ≤ 24 chars |
| `social` | `string[]` | ✔ | 1–4 items; ≤ 24 chars |
| `technological` | `string[]` | ✔ | 1–4 items; ≤ 24 chars |
| `insight` | `string` | ✔ | ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Include only external factors that could actually affect our business (not a general news roundup)
- Distinguish facts (enacted regulations, published statistics) from expectations (under deliberation, early signs), and attach a timeframe to expectations
- Do not judge opportunity vs. threat here (pass that judgment to SWOT — this slide sticks to describing the environment)
- Include at least one item in each of the four quadrants. An empty quadrant reads as "not researched"
- Consolidate the source of each factor in source (official gazette, statistics, research report name)

### Five Forces Analysis (`five-forces`)

![Five Forces Analysis](images/slide-templates/five-forces.png)

Evaluates industry competitive structure through five forces (rivalry, new entrants, substitutes, buyers, suppliers)

**Answers**: Where does competitive pressure in this industry come from, and how strong is it?

**figures**: `governing_message`, `cards`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `rivalry` | `string` | ✔ | ≤ 36 chars |
| `entrants` | `string` | ✔ | ≤ 40 chars |
| `substitutes` | `string` | ✔ | ≤ 40 chars |
| `buyers` | `string` | ✔ | ≤ 36 chars |
| `suppliers` | `string` | ✔ | ≤ 36 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- Write each force as "Strong / Medium / Weak: rationale" (don't give just the rating or just the rationale). title carries the conclusion — which force squeezes profitability the most
- State clearly in title or source which industry (market scope) this covers. The rating changes if the scope changes
- The five forces describe industry structure. Do not write our own company-specific strengths or weaknesses (send those to swot-analysis)
- The rating includes the evaluator's judgment. State the source material for the rating and the evaluation date in source
- The center force (industry rivalry) is also a result of the other four. This diagram does not assert causal arrows

### Issue Priority Matrix (`priority-matrix`)

![Issue Priority Matrix](images/slide-templates/priority-matrix.png)

Plots issues on two axes — impact and ease of execution — showing where to start

**Answers**: Among the issues we've identified, which should we tackle first?

**figures**: `governing_message`, `posmap`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `items` | `array` (string≤10, number≤1, number≤1) | ✔ | 2–8 items |
| `highlight` | `string[]` | — | ≤ 3 items; ≤ 10 chars; default `[]` |
| `xAxis` | `string[]` | — | 2 items; ≤ 12 chars; default `["Hard to do", "Easy to…` |
| `yAxis` | `string[]` | — | 2 items; ≤ 12 chars; default `["Low impact", "High im…` |
| `insight` | `string` | ✔ | ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- items is [issue name, x, y]. x / y are relative values from 0 to 1 (0 = left/bottom, 1 = right/top). State the basis for the rating (impact estimate, effort estimate), the evaluator, and the evaluation date in source
- Keep the issue name short enough to fit the bubble (0.8in diameter ≈ 4 characters/line). For 5 or more characters, mark the line break explicitly with \n, e.g. "Access request\nautomation"
- The top-right quadrant (easy to execute × high impact) is highest priority. But since the coordinates tend to be subjective, use this to agree on relative order rather than debating exact position
- Overlapping bubbles become unreadable. Limit to 8 issues, and merge issues that sit close together
- Use highlight only for issues we've decided to act on (too much emphasis emphasizes nothing)
- This slide does not explain the substance of each issue (what it solves). Define that in gap-analysis or logic-tree before bringing it here

<a id="read-alone"></a>

## Read-Alone Pack (`read-alone`)

Pages of high-density slides that can be read to completion on a single page (consulting-firm-style handouts). Each fits a governing message, lead-in, evidence, implication, and source onto one slide. Every template carries $density variants, so the same file renders at two densities: print (handout) and presentation (stage).

### Claim and Evidence Table (`claim-evidence-table`)

![Claim and Evidence Table](images/slide-templates/claim-evidence-table.png)

The standard read-alone form: claim, supporting evidence table, implication, and source, all closed on one page

**Answers**: Which facts support this claim?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `headers` | `string[]` | ✔ | 2–3 items; ≤ 10 chars |
| `rows` | `string[][]` | ✔ | 3–(print 7 · presentation 4) items; ≤ (print 24 · presentation 16) chars |
| `insight` | `string` | ✔ | ≤ (print 90 · presentation 50) chars |
| `insightPoints` | `string[]` | — | ≤ (print 3 · presentation 2) items; ≤ (print 24 · presentation 18) chars; default `[]` |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Write the title as "what can be said" in two lines or fewer; never as a caption describing the table
- Keep the implication within what the table shows. Never restate the title or introduce new information
- Never show a number without a source. Cut any row you cannot source

### Exhibit and Implication (`exhibit-analysis`)

![Exhibit and Implication](images/slide-templates/exhibit-analysis.png)

Puts a chart inside a numbered exhibit frame with the reading on the right, making one page referable from the body and appendix

**Answers**: What can be read from this exhibit?

**figures**: `governing_message`, `lead_in`, `exhibit_frame`, `vbars`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `exhibitNumber` | `integer` | — | 1–99; default `1` |
| `exhibitTitle` | `string` | ✔ | ≤ 40 chars |
| `bars` | `array` (string≤(print 5 · presentation 6), number) | ✔ | 2–(print 8 · presentation 5) items |
| `unit` | `string` | — | ≤ 8 chars; default `""` |
| `insight` | `string` | ✔ | ≤ (print 90 · presentation 50) chars |
| `insightPoints` | `string[]` | — | ≤ (print 3 · presentation 2) items; ≤ (print 24 · presentation 18) chars; default `[]` |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- The exhibit number exists for cross-reference from the body and appendix; sequential numbering is the author's responsibility
- Keep the implication within what the chart shows; never add information that is not in the figure
- Bar chart baselines are fixed at zero. Never exaggerate an axis to inflate a difference

### Executive Summary (`exec-summary-readable`)

![Executive Summary](images/slide-templates/exec-summary-readable.png)

Situation, complication, and resolution in three tiers plus supporting points, so the first page alone enables a decision

**Answers**: What, in the end, is this document asking the reader to decide?

**figures**: `governing_message`, `exec_summary`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `situation` | `string` | ✔ | ≤ (print 80 · presentation 48) chars |
| `complication` | `string` | ✔ | ≤ (print 80 · presentation 48) chars |
| `resolution` | `string` | ✔ | ≤ (print 80 · presentation 48) chars |
| `points` | `string[]` | — | ≤ (print 4 · presentation 3) items; ≤ (print 46 · presentation 28) chars; default `[]` |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- The condition is that this one page alone enables the decision; the body serves as supporting evidence
- Write the resolution tier as a conclusion with numbers; never end on "to be considered"
- If the supporting points exceed five, rework the body's chapter structure instead

### Conclusion, Rationale, and So What (`conclusion-rationale-implication`)

![Conclusion, Rationale, and So What](images/slide-templates/conclusion-rationale-implication.png)

Leads with the conclusion, then rationale cards and the next action (So What), read as a one-page pyramid

**Answers**: What is the conclusion, why does it hold, and what happens next?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rationale` | `array` (string≤12, string≤(print 48 · presentation 30)) | ✔ | 3–(print 4 · presentation 3) items |
| `implication` | `string` | ✔ | ≤ (print 100 · presentation 60) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Put the conclusion in the title; never make the reader reach the end of the body to find it
- Limit the rationale to 3-4 non-overlapping points; split to another slide beyond five
- So What states the next action; never a restatement of the rationale or the title

### Option Comparison Matrix (`dense-comparison-table`)

![Option Comparison Matrix](images/slide-templates/dense-comparison-table.png)

Compares options in a dot-rating matrix and shows the recommendation with its reasoning on one page

**Answers**: Which option is best, and why recommend it?

**figures**: `governing_message`, `lead_in`, `rating_matrix`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `criteria` | `string[]` | ✔ | 3–(print 5 · presentation 4) items; ≤ 8 chars |
| `options` | `array` (string≤16, integer[]≤4) | ✔ | 2–(print 5 · presentation 4) items |
| `recommendation` | `string` | ✔ | ≤ (print 100 · presentation 60) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Limit criteria to those that affect the decision; never pad with filler criteria
- Always state the basis for the scoring (quotes, evaluation sessions) in the source
- Write the recommendation as an overall judgment that concedes weaknesses; never disguise it as a clean sweep

### Storyline (`key-message-storyline`)

![Storyline](images/slide-templates/key-message-storyline.png)

Lines up the body's action titles in order — the horizontal logic — showing on one page that the titles alone carry the argument

**Answers**: Does the argument hold when reading only the titles?

**figures**: `governing_message`, `lead_in`, `storyline`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `messages` | `string[]` | ✔ | 4–(print 8 · presentation 5) items; ≤ (print 40 · presentation 26) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Each row must match the action title of its body slide; never rewrite as a summary
- If the argument breaks here, fix the structure before building the slides
- Write each row as a claim ("what can be said"); never as a table-of-contents topic

### Anticipated Q&A (`faq-objection-handling`)

![Anticipated Q&A](images/slide-templates/faq-objection-handling.png)

Pairs the objections expected at the approval meeting with sourced answers, preempting the Q&A on one page

**Answers**: How will we answer the expected objections?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `headers` | `string[]` | — | 2 items; ≤ 12 chars; default `["Likely concern", "Ans…` |
| `qa` | `string[][]` | ✔ | 3–(print 5 · presentation 3) items; ≤ (print 50 · presentation 30) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Every answer must carry its basis (exhibit numbers, appendix, test results)
- Leave unanswerable concerns visible; a glossed-over answer destroys the document's credibility
- Limit concerns to ones that will actually come up; never stage weak strawman objections

### One-Page Brief (`one-page-brief`)

![One-Page Brief](images/slide-templates/one-page-brief.png)

The densest page in the pack: exhibit chart, comparison table, implication, decision items, and source, all on one approval-ready page

**Answers**: Can the approver confirm the judgment and the decision items from this one page alone?

**figures**: `governing_message`, `lead_in`, `exhibit_frame`, `vbars`, `table`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `exhibitNumber` | `integer` | — | 1–99; default `1` |
| `exhibitTitle` | `string` | ✔ | ≤ 30 chars |
| `bars` | `array` (string≤(print 5 · presentation 6), number) | ✔ | 2–(print 6 · presentation 4) items |
| `unit` | `string` | — | ≤ 8 chars; default `""` |
| `headers` | `string[]` | ✔ | 2–3 items; ≤ 8 chars |
| `rows` | `string[][]` | ✔ | 3–(print 5 · presentation 3) items; ≤ (print 16 · presentation 12) chars |
| `implication` | `string` | ✔ | ≤ (print 55 · presentation 28) chars |
| `decisions` | `string[][]` | ✔ | 1–2 items; ≤ (print 22 · presentation 16) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- This page is the final check for approval. The exhibit and table summarize the body; never introduce a first-appearance number here
- Write decision items so Yes/No can be said, with a deadline. "Continue considering" is not a decision item
- Pack in information, not text. Keep table cells short and nominal

<a id="business-plan"></a>

## Business Plan Pack (`business-plan`)

Pages covering what an approver looks at first in a business plan or investment proposal: revenue, investment, risk, and the team. Multi-year P/L, the revenue bridge, cost structure, break-even, scenario comparison, payback, the risk register with exit criteria, and the execution structure, each as a one-page format. Every template carries $density variants, so the same file renders as a written plan document (print) or a board-meeting slide (presentation).

### Revenue Plan (multi-year P/L) (`revenue-plan`)

![Revenue Plan (multi-year P/L)](images/slide-templates/revenue-plan.png)

Shows the profit plan from revenue down to operating income as a year-by-year table, making the level and timing of profitability visible on one page

**Answers**: What revenue and profit will this business reach in each year, and when does it turn profitable?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `headers` | `string[]` | ✔ | 5 items; ≤ 8 chars |
| `rows` | `string[][]` | ✔ | 3–(print 7 · presentation 5) items; ≤ (print 9 · presentation 8) chars |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- The first column is the line item and the remaining four are periods. For a three-year plan, use the last column for a cumulative total or CAGR (never leave an empty column)
- State the unit (millions, thousands) and whether figures are tax-exclusive in the first header or the lead-in
- Revenue is substantiated by sales-buildup and costs by cost-structure. This slide carries only the result
- Ratio rows such as margins take pre-computed strings. Nothing is calculated on the slide
- These are plan values, not actuals. If actuals appear alongside, mark the row label so the two are distinguishable
- Every figure moves when the assumptions move. Show the range of assumptions separately in scenario-comparison

### Revenue Bridge (driver buildup) (`sales-buildup`)

![Revenue Bridge (driver buildup)](images/slide-templates/sales-buildup.png)

Bridges from a starting point to the planned figure driver by driver, showing what the growth is made of and what it depends on

**Answers**: Which drivers add up to the planned revenue, and which one does it depend on most?

**figures**: `governing_message`, `lead_in`, `waterfall`, `so_what`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `array` (string≤8, number, string≤5) | ✔ | 3–(print 7 · presentation 5) items |
| `unit` | `string` | — | ≤ 6 chars; default `""` |
| `implication` | `string` | ✔ | ≤ (print 100 · presentation 70) chars |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- The third element of each item is total (a sum stacked from zero) or delta (a change from the previous total). The first item must be a total (the starting point)
- The final total must equal the accumulated deltas or generation fails. Transcribe the value from the spreadsheet
- A bridge whose running sum goes negative cannot be drawn. Split the starting point or use a separate slide when the decline is large
- Cut drivers at a granularity someone can act on (new, expansion, churn, price). Do not leave a large lump inside "other"
- State the unit in unit or the lead-in. Put the derivation of each driver in the source line
- The total of the buildup must match the revenue in revenue-plan

### Investment and Cost Structure (`cost-structure`)

![Investment and Cost Structure](images/slide-templates/cost-structure.png)

Places the year-by-year cost mix beside the up-front/recurring breakdown by line item to show how much the business needs

**Answers**: How much does this business need, and what is the money spent on and in what order?

**figures**: `governing_message`, `lead_in`, `vbars_stacked`, `table`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `categories` | `string[]` | ✔ | 2–4 items; ≤ 6 chars |
| `series` | `array` (string≤5, number[]) | ✔ | 2–4 items |
| `breakdown` | `string[][]` | ✔ | 2–(print 6 · presentation 4) items; ≤ (print 7 · presentation 5) chars |
| `unit` | `string` | — | ≤ 6 chars; default `""` |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- At most four cost categories. Fold the fifth and beyond into "other" or the stack becomes unreadable
- The total of the stack must match cost of sales plus SG&A in revenue-plan
- Always split up-front (one-time) from annual recurring in the right-hand table. Never show only a combined total
- Put an em dash in cells that do not apply rather than leaving them blank, so they read as deliberate
- Write the headcount x unit-cost assumption behind personnel cost in the source line. An amount alone cannot be assessed
- State the unit and whether figures are tax-exclusive in the lead-in or unit
- At most four periods. Aggregate the years or split into two slides beyond that

### Break-Even Analysis (`break-even`)

![Break-Even Analysis](images/slide-templates/break-even.png)

Marks the break-even point where the revenue line meets the total-cost line, with the margin of safety against plan alongside

**Answers**: How much must we sell to stop losing money, and how much room does the plan have?

**figures**: `governing_message`, `lead_in`, `linechart`, `metric`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `labels` | `string[]` | ✔ | 3–5 items; ≤ 5 chars |
| `series` | `array` (string≤8, number[]) | ✔ | 2–3 items |
| `unit` | `string` | — | ≤ 6 chars; default `""` |
| `bepValue` | `string` | ✔ | ≤ 8 chars |
| `bepLabel` | `string` | — | ≤ 14 chars; default `"Break-even revenue"` |
| `marginValue` | `string` | ✔ | ≤ 8 chars |
| `marginLabel` | `string` | — | ≤ 14 chars; default `"Margin of safety (vs p…` |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- Always state the fixed/variable cost split definition in the source line. A different split moves the break-even point
- Draw the revenue and total-cost lines on the same horizontal axis (volume or revenue scale) and the same period
- This is a single-year static analysis and says nothing about cash. Payback and cash belong in roi-payback
- The straight-line approximation breaks down where the variable-cost ratio changes by volume band. State the applicable range in the lead-in
- Margin of safety is (plan revenue - break-even revenue) / plan revenue. Pass a pre-computed string
- Put the plan point (the current sales level) into words in the lead-in or source. Do not label the line ends: they always overlap near the crossing point

### Scenario Comparison (low / base / high) (`scenario-comparison`)

![Scenario Comparison (low / base / high)](images/slide-templates/scenario-comparison.png)

Shows several scenarios' trajectories with the assumptions that separate them, making the plan's range explicit

**Answers**: How far do results move when the assumptions move, and is the worst case acceptable?

**figures**: `governing_message`, `lead_in`, `linechart`, `table`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `labels` | `string[]` | ✔ | 3–6 items; ≤ 6 chars |
| `series` | `array` (string≤8, number[]) | ✔ | 2–3 items |
| `unit` | `string` | — | ≤ 6 chars; default `""` |
| `assumptions` | `string[][]` | ✔ | 2–3 items; ≤ (print 12 · presentation 9) chars |
| `source` | `string` | ✔ | ≤ 80 chars |
| `yMax` | `number` | — | ≥ 0; default `null` |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- Scenarios differ only by their assumptions. Never derive different results from the same assumptions
- List only the drivers that move the result. Do not include assumptions that stay fixed
- The low scenario must be bad enough to change a decision, not merely "a bit worse". Keep the high case at a defensible ceiling
- The base scenario's figures must match revenue-plan
- Do not weight the scenarios by probability. Argue likelihood on a separate slide with evidence
- When this drives an exit decision, tie the scenarios to the exit criteria in risk-register
- Set yMax to the round number just above the maximum. The automatic scale can reach 1.4x the maximum, squashing every line into the lower half of the plot

### Payback (ROI and payback period) (`roi-payback`)

![Payback (ROI and payback period)](images/slide-templates/roi-payback.png)

Shows when the investment is recovered at the crossing of cumulative investment and cumulative return, with return rate, payback period, and NPV alongside

**Answers**: When is the money recovered, and does this hold up as an investment decision?

**figures**: `governing_message`, `lead_in`, `metric`, `linechart`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `roiValue` | `string` | ✔ | ≤ 8 chars |
| `roiLabel` | `string` | — | ≤ 14 chars; default `"ROI (cumulative)"` |
| `paybackValue` | `string` | ✔ | ≤ 8 chars |
| `paybackLabel` | `string` | — | ≤ 14 chars; default `"Payback period"` |
| `npvValue` | `string` | ✔ | ≤ 8 chars |
| `npvLabel` | `string` | — | ≤ 14 chars; default `"NPV (net present value…` |
| `labels` | `string[]` | ✔ | 3–6 items; ≤ 6 chars |
| `series` | `array` (string≤8, number[]) | ✔ | 2–3 items |
| `unit` | `string` | — | ≤ 6 chars; default `""` |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- Compare cumulative against cumulative. Never mix a single-year figure into the same chart as a cumulative one
- Draw both series in a form that stays non-negative (cumulative investment and cumulative return) and read the crossing as the recovery point
- Always state the discount rate and horizon in npvLabel or the source line. An NPV without its assumptions cannot be compared
- State whether the payback period is undiscounted or discounted. The two can differ by more than a year
- Return rate and NPV take pre-computed strings. Nothing is calculated on the slide
- The investment must match cost-structure and the revenue behind the return must match revenue-plan
- Do not label the line ends: they overlap near the crossing point. Show the amounts in the metric tiles above instead

### Risks, Mitigations, and Exit Criteria (`risk-register`)

![Risks, Mitigations, and Exit Criteria](images/slide-templates/risk-register.png)

Lists the major risks with impact and likelihood, paired with the mitigation and the trigger for exiting or revisiting

**Answers**: What could break the plan, how is it handled, and what makes us stop?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `risks` | `string[][]` | ✔ | 2–(print 5 · presentation 4) items; ≤ (print 22 · presentation 16) chars |
| `decisionRule` | `string` | ✔ | ≤ (print 110 · presentation 70) chars |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- One row per risk. Use the same vocabulary for impact and likelihood across every row (high/medium/low)
- Exit and revisit criteria must be observable numbers. "Depending on the situation" is not a criterion
- A mitigation is a concrete action an owner can take. "Be careful" and "consider it" are not mitigations
- Leave out risks already mitigated down to a small residual. Keep only risks that could change an approver's decision
- Put who decides and when into decisionRule. Do not mix the per-row criteria with the division of responsibility
- When used with scenario analysis, align the exit criteria with the level of the low scenario

### Execution Structure and Roles (`execution-structure`)

![Execution Structure and Roles](images/slide-templates/execution-structure.png)

Places the delivery org chart beside the owner and headcount per role to substantiate that the plan can actually be executed

**Answers**: Who executes this business, and how many people does each role need?

**figures**: `governing_message`, `lead_in`, `orgchart`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `tree` | `tuple` (string≤18, array) | ✔ | — |
| `roles` | `string[][]` | ✔ | 2–(print 6 · presentation 4) items; ≤ (print 15 · presentation 12) chars |
| `notes` | `string[]` | — | ≤ 2 items; ≤ 60 chars; default `[]` |
| `source` | `string` | ✔ | ≤ 80 chars |

Guardrails:

- Keep the title to roughly 30 full-width characters on one line. A second line collides with the lead-in below it
- Draw the structure for this business, not the company org chart. Leave out departments that are not involved
- Headcount must match the personnel cost in cost-structure. Do not add people here and forget them in the cost plan
- Always distinguish dedicated from shared assignments. A structure heavy on shared assignments draws feasibility challenges
- Depth of four or more, or over eight leaves, crushes the text. Split into a slide per department
- Two-line node labels ("role\nheadcount") read best. Mask individual names when the deck leaves the company
- When the structure differs between launch and scale-up, state the period and use two slides

<a id="nexus"></a>

## Nexus Report Pack (`nexus`)

Pages that turn a nexus-architect run into slides: what the analysis covered, what it found, what was decided and what is still open. Every page states the basis it rests on.

### Pipeline Coverage (`pipeline-coverage`)

![Pipeline Coverage](images/slide-templates/pipeline-coverage.png)

States up front how far the analysis behind this deck actually got (done / running / not started)

**Answers**: How much completed analysis is this proposal or report based on?

**figures**: `governing_message`, `lead_in`, `hbars`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `basis` | `string` | ✔ | ≤ (print 150 · presentation 110) chars |
| `counts` | `array` (string≤12, number, string≤8) | ✔ | 2–5 items |
| `headers` | `string[]` | ✔ | 3 items; ≤ 14 chars |
| `groups` | `string[][]` | ✔ | 2–(print 7 · presentation 5) items; ≤ (print 22 · presentation 16) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Always the second slide. A deck explaining incomplete work misleads the reader unless the basis is disclosed first
- counts are actual phase counts. Do not reinterpret them as report pages or hours worked
- Do not relabel a not-started phase as 'out of scope'. skipped (deliberately passed over) and pending (not yet) are different
- Always put the collection time (asOf) in source. The pipeline keeps moving, so the deck is always a snapshot

### Phase Digest (section divider) (`phase-digest`)

![Phase Digest (section divider)](images/slide-templates/phase-digest.png)

Shows what one phase established, using that phase's own summary and its output files

**Answers**: What did this phase establish, and which report says so?

**figures**: `governing_message`, `lead_in`, `so_what`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `phase` | `string` | ✔ | ≤ 60 chars |
| `finding` | `string` | ✔ | ≤ (print 200 · presentation 150) chars |
| `headers` | `string[]` | ✔ | 2 items; ≤ 16 chars |
| `outputs` | `array` (string≤42, string≤(print 46 · presentation 34)) | ✔ | 1–3 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Base summary on what the phase recorded itself (the summary in pipeline-progress.json). Rewording for the reader is fine; adding a new claim is not
- outputs lists files that exist. Anything declared but unwritten goes to open-questions
- A section divider carries one conclusion. Split multiple findings onto following pages

### Technology Stack Inventory (`stack-inventory`)

![Technology Stack Inventory](images/slide-templates/stack-inventory.png)

Shows what the current system is built on, layer by layer, with versions and constraints

**Answers**: What is this system standing on?

**figures**: `governing_message`, `lead_in`, `layers`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `layers` | `array` (string≤22, string≤(print 52 · presentation 40)) | ✔ | 3–(print 6 · presentation 5) items |
| `facts` | `string[][]` | ✔ | 2–(print 7 · presentation 5) items; ≤ (print 24 · presentation 18) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Layers are what actually runs. Anything present in config but never called is marked 'declared only' in the note
- Always give the version. Support windows and known vulnerabilities cannot be judged without it
- No judgement (good/bad) on this page. Problems go to issue-register

### Issue, Debt and Finding Register (`issue-register`)

![Issue, Debt and Finding Register](images/slide-templates/issue-register.png)

Shows the detected issues as a severity distribution plus individual entries

**Answers**: What has been found, and how bad is it?

**figures**: `governing_message`, `lead_in`, `hbars`, `table`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `counts` | `array` (string≤12, number, string≤8) | ✔ | 2–4 items |
| `issues` | `string[][]` | ✔ | 2–(print 7 · presentation 5) items; ≤ (print 24 · presentation 18) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Use the severity the report assigned. Do not raise or lower it in the deck
- The total of counts and the number of issues must agree (say so in the lead if this is an extract)
- No remedies on this page. This page goes as far as 'what was found'
- Every entry keeps its evidence (file, line, endpoint)

### Evaluation Score (single measure) (`score-card`)

![Evaluation Score (single measure)](images/slide-templates/score-card.png)

Shows a single maturity or readiness score with its bands, and lists the components

**Answers**: Where does this measure put us right now?

**figures**: `governing_message`, `lead_in`, `metric`, `hbars`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `score` | `string` | ✔ | ≤ 10 chars |
| `scoreCaption` | `string` | ✔ | ≤ (print 48 · presentation 34) chars |
| `dimensions` | `array` (string≤18, number, string≤10) | ✔ | 2–(print 5 · presentation 4) items |
| `verdict` | `string` | ✔ | ≤ (print 150 · presentation 110) chars |
| `source` | `string` | ✔ | ≤ 170 chars |
| `scale` | `number` | — | ≥ 0.1; default `5` |

Guardrails:

- Always show the score with its bands (what a given score means). A number alone cannot be judged
- Component names and weights come from the report's definition. Do not add or round them in the deck
- verdict says only what the number supports. Improvements go to roadmap / decision-record
- Bars are drawn against scale (default 5) as full. When the indicator uses a different scale, set scale to match

### Score Breakdown (subject x dimension) (`score-breakdown`)

![Score Breakdown (subject x dimension)](images/slide-templates/score-breakdown.png)

Compares scores per module and dimension as a surface, showing what is holding the total down

**Answers**: Which subject, on which dimension, is the low one?

**figures**: `governing_message`, `lead_in`, `rating_matrix`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `columns` | `string[]` | ✔ | 2–(print 6 · presentation 5) items; ≤ 14 chars |
| `rows` | `array` (string≤20, integer[]≤5) | ✔ | 2–(print 5 · presentation 4) items |
| `verdict` | `string` | ✔ | ≤ (print 100 · presentation 75) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Row and column order follows the report. If you sort by score, say so
- Scores are integers from 0 to levels. If you round a fractional score, put the rounding rule in source
- Do not make blank (not evaluated) look like 0. Either drop the row or label it in the row heading

### Context Map (`context-map`)

![Context Map](images/slide-templates/context-map.png)

Shows the central context and its neighbours, with the relationship type on each edge

**Answers**: Which boundary is central, and how does it relate to its neighbours?

**figures**: `governing_message`, `lead_in`, `hub`, `table`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `center` | `string` | ✔ | ≤ 22 chars |
| `contexts` | `string[]` | ✔ | 3–6 items; ≤ 24 chars |
| `relations` | `string[][]` | ✔ | 2–(print 6 · presentation 4) items; ≤ (print 22 · presentation 18) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Relationship types (Shared Kernel / Conformist / Separate Ways ...) use the report's terms as they stand
- Do not mix As-Is and To-Be on one page. State which it is in the title
- Many-to-many relationships that the diagram cannot carry go into the table. Do not add lines until it is unreadable
- To paste the original mermaid as it is, use architecture-exhibit

### Architecture Exhibit (image plus what to read from it) (`architecture-exhibit`)

Pastes the report's structural diagram as an exhibit and adds three things to read from it

**Answers**: What am I supposed to read from this diagram?

**figures**: `governing_message`, `exhibit_frame`, `image`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `exhibitNumber` | `string` | ✔ | ≤ 6 chars |
| `exhibitTitle` | `string` | ✔ | ≤ 44 chars |
| `image` | `string` | ✔ | ≤ 200 chars |
| `lead` | `string` | ✔ | ≤ (print 90 · presentation 70) chars |
| `readings` | `string[]` | ✔ | 2–3 items; ≤ (print 34 · presentation 26) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Use an image exported by scripts/mermaid_export.py / drawio_export.py / html_shot.py, and inspect it with Read after generating
- The frame is about 2:1. An extremely tall or wide diagram leaves the page mostly margin, so split it or redraw it natively
- Do not paste a diagram that cannot be read at slide size. Split it or redraw it natively
- readings state only what the diagram shows. Do not slip a claim the diagram does not carry into the three points
- Number exhibits sequentially so the body text can refer to them

### Decision and Rationale (ADR) (`decision-record`)

![Decision and Rationale (ADR)](images/slide-templates/decision-record.png)

Keeps the options considered, what was adopted or rejected, and why, on one page

**Answers**: Why was this approach chosen, and on what grounds were the others dropped?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `question` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `options` | `string[][]` | ✔ | 2–4 items; ≤ (print 34 · presentation 26) chars |
| `decision` | `string` | ✔ | ≤ (print 150 · presentation 110) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Always show at least one rejected option. A table of the adopted option alone is advertising, not a decision record
- Tie the rationale to evaluation dimensions (performance, consistency, operations, cost). Not to preference
- If the decision is not settled, write 'undecided'. Do not make it look decided
- Include what the decision affects (which phases depend on it) in decision

### Migration and Implementation Roadmap (`roadmap`)

![Migration and Implementation Roadmap](images/slide-templates/roadmap.png)

Shows the sequence, the durations and the dependencies as bands

**Answers**: What gets done, in what order, over what period?

**figures**: `governing_message`, `lead_in`, `gantt`, `source_note`  
**Inference level**: Strategic (evaluation and direction-setting)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `columns` | `string[]` | ✔ | 3–(print 8 · presentation 6) items; ≤ 10 chars |
| `rows` | `array` (string≤(print 24 · presentation 18), number, number, string≤18) | ✔ | 2–(print 7 · presentation 5) items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- If there is no basis for a duration, do not give one. Make it a relative-order diagram instead
- Bands that run in parallel must actually be able to (no dependency between them)
- Milestones (start == end) mark decision points, not work
- If a step depends on a phase that has not started, say so in the lead

### Persona and Where the Experience Stalls (`persona-journey`)

![Persona and Where the Experience Stalls](images/slide-templates/persona-journey.png)

Lays out the target user's experience in sequence and shows where it stalls

**Answers**: Who stalls, in which experience, and where?

**figures**: `governing_message`, `lead_in`, `journey`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `persona` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `milestones` | `array` (string≤14, string≤(print 34 · presentation 24)) | ✔ | 3–5 items |
| `pains` | `array` (string≤18, string≤(print 50 · presentation 38)) | ✔ | 2–3 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Persona attributes are only what the research confirmed. Do not invent an age or an income to fill the card
- Tie each pain to an observed fact (something said, done, or measured). If it is inference, label it a hypothesis
- No solutions on this page. This page goes as far as the experience as it is

### Screen Flow (three UI mocks) (`ui-mock-flow`)

Shows the flow of an experience as screen mocks ordered by the domain story

**Answers**: Which screens does this work pass through, and in what order?

**figures**: `governing_message`, `lead_in`, `image`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `story` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `shot1` | `string` | ✔ | ≤ 200 chars |
| `shot2` | `string` | ✔ | ≤ 200 chars |
| `shot3` | `string` | ✔ | ≤ 200 chars |
| `steps` | `array` (string≤16, string≤(print 44 · presentation 34)) | ✔ | 3 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Order the mocks by the story ({STORY}-NN-{slug}.html, by NN). Do not reorder them for looks
- Capture with scripts/html_shot.py and inspect with Read before pasting (a capture succeeds even when the styles did not load)
- A mock is a specification, not an implementation. Do not word it so it reads as 'already built'
- A flow that does not fit in three screens is split across pages at a boundary that does fit

### Screen Detail (one UI mock) (`ui-mock-detail`)

Shows one screen mock large, with the points to notice and the specification beside it

**Answers**: What has been settled on this screen?

**figures**: `governing_message`, `lead_in`, `image`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `screen` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `shot` | `string` | ✔ | ≤ 200 chars |
| `notes` | `string[][]` | ✔ | 2–(print 6 · presentation 4) items; ≤ (print 18 · presentation 14) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- The points to notice are only what is visible on the screen. Processing behind it does not go on this page
- Capture with scripts/html_shot.py and inspect with Read before pasting
- Mark anything unsettled as 'undecided' in the table. Do not present what the mock happens to draw as settled

### Open Questions and Next Actions (`open-questions`)

![Open Questions and Next Actions](images/slide-templates/open-questions.png)

Shows the premises still unfilled and the phases not started, along with who fills them and how

**Answers**: What can this material not yet answer, and how does that get filled?

**figures**: `governing_message`, `lead_in`, `table`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `headers` | `string[]` | ✔ | 3 items; ≤ 16 chars |
| `questions` | `string[][]` | ✔ | 2–(print 5 · presentation 4) items; ≤ (print 36 · presentation 28) chars |
| `nextSteps` | `array` (string≤18, string≤(print 50 · presentation 38)) | ✔ | 2–3 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Do not remove an item from here by filling it with a guess. It is open because it is unfilled, not because nothing could be written
- Every row says who fills it and with which command. Do not leave it as a list of homework
- Do not mix open questions arising from unstarted phases with ones the report stated explicitly (distinguish them in source)
- On a partially-run deck, this is always the last page

### Bounded Context Canvas (`bounded-context-canvas`)

![Bounded Context Canvas](images/slide-templates/bounded-context-canvas.png)

Shows one bounded context as a nine-part canvas: what it is responsible for and where its boundary ends

**Answers**: What is this context responsible for, and where does its boundary end?

**figures**: `governing_message`, `lead_in`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `purpose` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `classification` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `evolution` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `domainRoles` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `inbound` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `outbound` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `language` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `decisions` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `assumptions` | `string` | ✔ | ≤ (print 54 · presentation 38) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Take the Core / Supporting / Generic classification straight from the report. Do not promote or demote it on the slide
- Leave what is undecided in 'assumptions'. An empty cell reads as settled
- Name the counterpart context in inbound and outbound. A pattern name (ACL, OHS) alone does not let the relationship be reconstructed
- One context per page. When you want to line several up, use context-map

### State x Event Verdict Matrix (`state-machine-matrix`)

![State x Event Verdict Matrix](images/slide-templates/state-machine-matrix.png)

Shows, for every combination of state and event, whether the aggregate accepts it or refuses it

**Answers**: Which events does each state accept, and which does it refuse?

**figures**: `governing_message`, `lead_in`, `table`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `headers` | `string[]` | ✔ | 3–6 items; ≤ 14 chars |
| `rows` | `string[][]` | ✔ | 2–(print 5 · presentation 4) items; ≤ 12 chars |
| `notes` | `array` (string≤18, string≤(print 50 · presentation 38)) | ✔ | 2 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Use only the four words allow / reject / ignore / defer, taken straight from the report
- Leave no cell empty. That every cell carries a verdict is the point of this table, and a blank cannot be told apart from 'not considered'
- When states or events exceed the cap, split the page and say so in source
- This table cannot carry a transition's effect or postcondition. Put those on their own page if they are needed

### Quality Gate Verdict (`gate-verdict`)

![Quality Gate Verdict](images/slide-templates/gate-verdict.png)

Brings the review verdict, the weighted score per perspective, and what remains by priority onto one page

**Answers**: Did the quality gate pass, and if it did not, what is still open?

**figures**: `governing_message`, `lead_in`, `metric`, `hbars`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `verdict` | `string` | ✔ | ≤ 22 chars |
| `score` | `string` | ✔ | ≤ 24 chars |
| `perspectives` | `array` (string≤14, number≤5, string≤8) | ✔ | 3–(print 5 · presentation 4) items |
| `headers` | `string[]` | ✔ | 3 items; ≤ 12 chars |
| `priorities` | `string[][]` | ✔ | 2–4 items; ≤ (print 30 · presentation 22) chars |
| `insight` | `string` | ✔ | ≤ (print 110 · presentation 80) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Do not write CONDITIONAL_PASS as PASS. A conditional pass means conditions are still open
- Carry the weighted score computed with review-registry.json's weights as it stands. Do not recompute it on the slide
- Do not summarise as 'passed' while P0 is not zero
- When perspectives are trimmed to the cap, name the ones left out in source

### Example Mapping (rules, examples, open questions) (`example-map-wall`)

![Example Mapping (rules, examples, open questions)](images/slide-templates/example-map-wall.png)

Shows, for one feature, the rules it must obey, the examples that check them, and the questions nobody settled

**Answers**: Which rules does this feature obey, which examples check them, and what is unsettled?

**figures**: `governing_message`, `lead_in`, `table`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `rules` | `string[][]` | ✔ | 2–(print 4 · presentation 3) items; ≤ (print 30 · presentation 22) chars |
| `examples` | `string[][]` | ✔ | 2–(print 4 · presentation 3) items; ≤ (print 24 · presentation 18) chars |
| `questions` | `array` (string≤18, string≤(print 46 · presentation 34)) | ✔ | 2 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Do not mix rules and examples. A rule is what always holds; an example is one case that checks it
- Do not show only positive examples. Without at least one example outside the boundary, the rule has not been checked
- Do not drop the open questions. Removing what was not settled makes it look settled
- A feature whose rules exceed the cap is a candidate for splitting. Put the number of rules left out in source

### Aggregate Boundary and Invariants (`aggregate-canvas`)

![Aggregate Boundary and Invariants](images/slide-templates/aggregate-canvas.png)

Shows what one aggregate contains and the invariants it keeps, together with the examples that check them

**Answers**: How far does this aggregate reach, and which invariants does it keep?

**figures**: `governing_message`, `lead_in`, `table`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `members` | `string[][]` | ✔ | 2–(print 4 · presentation 3) items; ≤ (print 22 · presentation 16) chars |
| `invariants` | `string[][]` | ✔ | 2–(print 4 · presentation 3) items; ≤ (print 24 · presentation 18) chars |
| `examples` | `array` (string≤18, string≤(print 46 · presentation 34)) | ✔ | 2 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Do not list members that sit outside the boundary. When it is only a reference, say so in the kind column
- Write only what is always true as an invariant. Procedures and business rules do not belong here
- Do not leave the violating-operation column empty. An invariant with no known violator cannot be verified
- One aggregate per page. Show relationships between aggregates with context-map or state-machine-matrix

### Screen x UX Axis Heatmap (`ux-heatmap`)

![Screen x UX Axis Heatmap](images/slide-templates/ux-heatmap.png)

Shows which screens and which evaluation axes the UX findings concentrate on

**Answers**: Which screen and which axis do the findings concentrate on?

**figures**: `governing_message`, `lead_in`, `rating_matrix`, `metric`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `axes` | `string[]` | ✔ | 3–5 items; ≤ 10 chars |
| `screens` | `array` (string≤16, integer[]≤4) | ✔ | 2–(print 5 · presentation 4) items |
| `uxi` | `string` | ✔ | ≤ 12 chars |
| `band` | `string` | ✔ | ≤ 22 chars |
| `insight` | `string` | ✔ | ≤ (print 110 · presentation 80) chars |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- The dot count is the number of findings, not their severity. Severity belongs on issue-register
- Take UXI and its band from ux-evaluation.json as they stand. Do not recompute them on the slide
- Do not drop a screen with zero findings. An empty row is the record that it was checked and nothing was wrong
- In an evaluation that was not measured at runtime, do not show axes about perceived speed

### Environment Parity (identical and different) (`env-parity-matrix`)

![Environment Parity (identical and different)](images/slide-templates/env-parity-matrix.png)

Shows what is kept identical across environments and what is deliberately different, with the reason

**Answers**: What is identical across environments, and where do they deliberately differ?

**figures**: `governing_message`, `lead_in`, `table`, `cards`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**densities**: print / presentation (default print)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ (print 38 · presentation 30) chars |
| `lead` | `string` | ✔ | ≤ (print 140 · presentation 100) chars |
| `headers` | `string[]` | ✔ | 3–5 items; ≤ 12 chars |
| `rows` | `string[][]` | ✔ | 2–(print 4 · presentation 3) items; ≤ (print 22 · presentation 16) chars |
| `identical` | `array` (string≤18, string≤(print 46 · presentation 34)) | ✔ | 2 items |
| `source` | `string` | ✔ | ≤ 170 chars |

Guardrails:

- Always give a reason for a difference. A difference with no reason is not a difference but an inconsistency nobody has fixed yet
- Do not leave 'what is kept identical' empty. A comparison that guarantees nothing between environments carries no meaning
- When environments exceed the cap, split the columns and say so in source
- Do not put an item that is unverified in production on the identical side. Leave it on the difference side with its reason

<a id="hearing"></a>

## Hearing Pack (`hearing`)

Pages whose job is to collect information rather than deliver it: the agenda of what you need to hear, your understanding put up to be corrected, a sheet to fill in during the meeting, a poll for an event, and where to send the answers.

### What We Need to Hear Today (`hearing-agenda`)

![What We Need to Hear Today](images/slide-templates/hearing-agenda.png)

Puts the open points on the table up front, each with the reason you are asking

**Answers**: What do we need to be told in this meeting?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `string[][]` | ✔ | 3–5 items; ≤ 40 chars |
| `ask` | `string` | ✔ | ≤ 60 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- If you cannot say why you are asking, leave it off. Questions are decided by order, not by volume
- Do not arrive with the blanks filled in by guesswork. You ask because it is blank
- Five items at most. Do not exceed what one meeting can answer

### Checking Our Understanding (`hypothesis-check`)

![Checking Our Understanding](images/slide-templates/hypothesis-check.png)

Puts your reading up as a hypothesis so the customer can correct what is wrong

**Answers**: Where is our understanding wrong?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `array` (string≤16, string≤68) | ✔ | 2–3 items |
| `ask` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **Do not assert.** Only rows whose confidence is 推定 belong here; listing confirmed ones checks nothing
- The point is to be corrected. Always end with 'tell us if we have this wrong'
- No figure without a source

### Blanks to Fill in Here (`fill-in-sheet`)

![Blanks to Fill in Here](images/slide-templates/fill-in-sheet.png)

Blank rows to write on during the meeting, on screen or on paper

**Answers**: What do we want filled in while we are in the room?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `string[][]` | ✔ | 3–6 items; ≤ 44 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- Leave the blanks blank. **Do not pre-fill a guess and call it 'please confirm'**
- Six items at most. Keep it to what there is time to write
- For anything they cannot answer now, ask who could

### Does Any of This Apply to You? (`event-poll`)

![Does Any of This Apply to You?](images/slide-templates/event-poll.png)

Lets an audience at a seminar or talk say which situation they are in

**Answers**: Which situation is this audience in?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `array` (string≤8, string≤56) | ✔ | 3–4 items |
| `ask` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- Write the options as buyer situations. **Do not make them choose by product name** — that is a product pitch, not collection
- Aggregate the answers as types. Do not keep who chose what in the material
- If you ask for an answer, say what comes back for it (a deck, a follow-up conversation)

### Where to Send the Answers (`collect-cta`)

![Where to Send the Answers](images/slide-templates/collect-cta.png)

Says where answers go, and what the customer gets back for sending them

**Answers**: Who sends the answers, where, and by when?

**figures**: `governing_message`, `lead_in`, `flow`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `steps` | `array` | ✔ | 2–4 items |
| `where` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses; check who the answer URL may be shared with before putting it on the page
- **Say what happens once they answer.** Collecting without returning anything teaches people not to answer
- Give a date and an owner. A request with neither does not move

### Answer Form (QR) (`collect-qr`)

![Answer Form (QR)](images/slide-templates/collect-qr.png)

Shows the QR for the answer destination large, for the end of a talk or a hand-off in person

**Answers**: Where do I go to answer this right now?

**figures**: `governing_message`, `lead_in`, `image`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `qr` | `string` | ✔ | ≤ 400 chars |
| `where` | `string` | ✔ | ≤ 120 chars |
| `source` | `string` | ✔ | ≤ 160 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses; confirm who can see what the QR points at before putting it up
- **Always spell the URL out as well.** There is always a seat where the QR will not scan
- If it points at a shared sheet, point at the customer version (internal columns and sections removed)

<a id="case-studies"></a>

## Case Study Pack (`case-studies`)

Pages that carry published customer cases into a deck: several in brief, one in depth, and why a case applies to the customer in front of you. Consent and dated sources are managed in templates/marketing/case-study.ja.md.

### Cases in Brief (three) (`case-study-card`)

![Cases in Brief (three)](images/slide-templates/case-study-card.png)

Three published cases, one line each — the page that reinforces trust after interest is there

**Answers**: How have other companies solved a problem like this?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `cases` | `array` (string≤16, string≤68) | ✔ | 2–3 items |
| `sowhat` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Only published cases.** No unpublished customer name or figure
- Use the figures as the published material states them, and **always put the source (outlet and date) in source**
- Cases and figures fall under the 3-month rule. **Re-check anything past its last-confirmed date before using it**
- Name a customer only within what was agreed with them (the deal side records the promise in stage 5 §6 / stage 6 §5)
- Three at most. **The more you line up, the weaker the impression.** Choose the ones closest to this audience

### One Case in Depth (`case-study-detail`)

![One Case in Depth](images/slide-templates/case-study-detail.png)

One customer, in the order problem -> what was done -> result, with published figures only

**Answers**: What was that company stuck on, what did they do, and what happened?

**figures**: `governing_message`, `lead_in`, `before_after`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `before` | `array` | ✔ | 2–3 items |
| `after` | `array` | ✔ | 2–3 items |
| `result` | `string` | ✔ | ≤ 90 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Only published cases.** No unpublished customer name or figure
- Use the figures as the published material states them, and **always put the source (outlet and date) in source**
- Cases and figures fall under the 3-month rule. **Re-check anything past its last-confirmed date before using it**
- Name a customer only within what was agreed with them (the deal side records the promise in stage 5 §6 / stage 6 §5)
- **Put before and after on the same dimension.** A before/after on shifting dimensions is not a comparison
- One headline figure. **Several at once leaves no impression of which one is the effect**

### Why This Case Applies to You (`case-fit`)

![Why This Case Applies to You](images/slide-templates/case-fit.png)

Sets the case's situation beside the customer's, showing why it is worth their attention

**Answers**: How far does that case actually apply to us?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 3–4 items; ≤ 38 chars |
| `caveat` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Only published cases.** No unpublished customer name or figure
- Use the figures as the published material states them, and **always put the source (outlet and date) in source**
- Cases and figures fall under the 3-month rule. **Re-check anything past its last-confirmed date before using it**
- Name a customer only within what was agreed with them (the deal side records the promise in stage 5 §6 / stage 6 §5)
- **Always name one thing that does not apply** (caveat). No case applies entirely, and hiding it surfaces during the PoC
- The customer's column carries only facts from the hearing. **Do not fill it with inference**

<a id="proposal"></a>

## Proposal Pack (`proposal`)

The sections of a problem-solving proposal that had no reusable page: the structure beneath the visible problems, the challenge-to-solution mapping, what is in and out of scope, how the work changes, the PoC and its pass criteria, and the next step.

### Challenge to Solution Mapping (`challenge-solution-map`)

![Challenge to Solution Mapping](images/slide-templates/challenge-solution-map.png)

Sets each agreed challenge against what solves it and what changes as a result — the core of the proposal

**Answers**: Which challenge is solved by what, and how?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Causal (asserting causes)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 2–4 items; ≤ 34 chars |
| `scope_out` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **Match the challenge slide in both order and wording.** Rephrased, it reads as a different subject
- One challenge per row. **Do not claim one capability solves three** (it breaks down under verification)
- A challenge you cannot solve gets no row; put it below as out of scope. **Do not force the fit**
- Write the result as what happens to the work, not as a restatement of the capability

### In Scope and Out of Scope (`scope-in-out`)

![In Scope and Out of Scope](images/slide-templates/scope-in-out.png)

Sets what is in against what is not, so expectations line up

**Answers**: How much of this is today's subject, and where does another conversation begin?

**figures**: `governing_message`, `lead_in`, `before_after`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `inside` | `array` | ✔ | 2–4 items |
| `outside` | `array` | ✔ | 2–4 items |
| `why` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **Decide what is out first.** A proposal with an empty out-of-scope grows afterwards
- Say why something is out. **'We won't' alone reads as 'we can't'**
- State the ranges another vendor or project already owns

### PoC Objective and Pass Criteria (`poc-plan`)

![PoC Objective and Pass Criteria](images/slide-templates/poc-plan.png)

What has to be confirmed for feasibility, and what happens after — as far as what follows a pass

**Answers**: What does the PoC confirm, and what happens if it passes?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 5–7 items; ≤ 50 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **Pass criteria are numeric or answerable yes/no.** 'It works well' is not a criterion
- **Always state what happens on a pass.** A PoC the customer cannot answer that for has not been designed yet
- Say what happens on a fail. A one-way PoC is not a verification
- State the people, effort and data needed from the customer. **Do not say 'no special burden'**

### Expected Outcome (how the work changes) (`outcome-before-after`)

![Expected Outcome (how the work changes)](images/slide-templates/outcome-before-after.png)

States the effect as a change in daily work rather than as a set of capabilities

**Answers**: What actually changes for the people doing the work?

**figures**: `governing_message`, `lead_in`, `before_after`, `so_what`, `source_note`  
**Inference level**: Predictive (projecting future values)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `before` | `array` | ✔ | 2–4 items |
| `after` | `array` | ✔ | 2–4 items |
| `quant` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **Write work, not capability.** Not 'it becomes ACID' but 'the reconciliation goes away'
- **Only quantified figures with a stated basis.** Without one, state it qualitatively and say it will be measured in the PoC
- Put before and after on the same dimension. A shifted comparison is no comparison
- Do not present a published case's figure as this customer's effect. Cases belong on their own page

### What to Decide Today, and the Next Step (`next-step-customer`)

![What to Decide Today, and the Next Step](images/slide-templates/next-step-customer.png)

Separates what is decided in the room from the homework each side takes away

**Answers**: What is being decided today, and who does what before the next meeting?

**figures**: `governing_message`, `lead_in`, `flow`, `so_what`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `steps` | `array` | ✔ | 2–4 items |
| `decide` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 2–3 items; ≤ 30 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- **One thing to decide today.** With several, none of them get decided
- Homework carries an owner and a date. **Without both it does not move**
- Always include something on our side. Not a page that only hands the customer homework

### Structure Beneath the Problems (`iceberg-challenge`)

![Structure Beneath the Problems](images/slide-templates/iceberg-challenge.png)

Separates the problems on the surface from the structure underneath, distinguishing the proposal from a symptomatic fix

**Answers**: What sits beneath the problems we can see?

**figures**: `governing_message`, `lead_in`, `iceberg`, `source_note`  
**Inference level**: Causal (asserting causes)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `above` | `array` | ✔ | 2 items |
| `below` | `array` | ✔ | 2–3 items |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Customer-facing.** No judgement about an individual's influence, stance or internal politics, and no competitor weaknesses
- The top is **what the customer actually said**; the bottom is our reading of the structure. **Do not mix them**
- The top takes **two short noun phrases** (10 characters each); only two lines fit above the waterline. Wanting three means the surface problems are not narrowed down yet
- Three at most below. **The more root causes you list, the less any of them looks like the root**
- Leave room for this reading to be wrong. Do not assert it before it is agreed

<a id="marketing"></a>

## Marketing Pack (`marketing`)

Pages aimed at people who are not yet in a deal: event and talk announcements, the value message for someone who does not yet see a problem, a single use case, and the abstract of a technical paper.

### Event and Seminar Announcement (`event-announce`)

![Event and Seminar Announcement](images/slide-templates/event-announce.png)

The essentials of an event — date, format, audience, price, how to register — on one page

**Answers**: When and where is this, who is it for, and how do I register?

**figures**: `governing_message`, `lead_in`, `event_mode_badge`, `event_overview`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 3–4 items; ≤ 44 chars |
| `mode` | `string` | ✔ | ≤ 10 chars |
| `cta` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **Only confirmed information.** Speakers, venue and time go on once they are settled, not before
- **Name the audience.** 'Anyone welcome' reaches nobody (and leaves the nurture stage undecided)
- Do not drop price, capacity or the registration deadline. **If they will not fit in four rows, combine them into one** (extra rows push out the registration line)
- mode is one of online / offline / hybrid
- The first column of rows is a pictogram name. Only the ones listed by scripts/illustrations.py --list exist (date=calendar, venue=pin, audience=people, price=coin, deadline=clock)

### Session Programme (`session-agenda`)

![Session Programme](images/slide-templates/session-agenda.png)

The running order, by time, subject and speaker

**Answers**: What is covered on the day, in what order, and by whom?

**figures**: `governing_message`, `lead_in`, `event_timetable`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 3–6 items; ≤ 40 chars |
| `note` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **Only confirmed times.** An unsettled slot says 'to be confirmed'; it is not left blank
- Six slots at most. Beyond that, split across two pages (do not shrink to fit)
- Always include time for questions. **Without it, nothing you needed to hear gets collected**

### Speakers (`speaker-intro`)

![Speakers](images/slide-templates/speaker-intro.png)

Who is speaking, and why this talk comes from them

**Answers**: Whose position does this talk come from?

**figures**: `governing_message`, `lead_in`, `event_speakers`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `speakers` | `array` (string≤14, string≤26, string≤30) | ✔ | 1–4 items |
| `why` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **Only confirmed details about real speakers.** Check the job title with the person
- Do not list a career. It is enough to convey why this person gives this talk
- A speaker from a customer company is handled like a case study: confirm the scope of their consent

### Improvement and Benefit (stages 0-1) (`value-message`)

![Improvement and Benefit (stages 0-1)](images/slide-templates/value-message.png)

Shows someone who does not yet see a problem what is worth settling at build or replacement time, and why it is worth it

**Answers**: What is worth fixing now so that something else does not happen later?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Diagnostic (identifying factors and structure)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `array` (string≤16, string≤60) | ✔ | 3 items |
| `ask` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **Stages 0-1 are the Why Change? stages. Do not load them with product detail** (nurture-map.md §1)
- Fix the three cards in the order situation -> the drawback they have not noticed -> what happens if left alone
- **Write in the buyer's words.** A sentence that opens with a product name is not read at this stage
- Do not assert. End with a question to the reader

### Use Case on One Page (stages 1-2) (`use-case-one-pager`)

![Use Case on One Page (stages 1-2)](images/slide-templates/use-case-one-pager.png)

One way of using the product, in the order preconditions -> what you do -> where it helps

**Answers**: What does using this concretely look like?

**figures**: `governing_message`, `lead_in`, `table`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 3–5 items; ≤ 50 chars |
| `fit` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **One page, one use case.** The more general it reads, the less it is anyone's
- Always state the preconditions this use holds under. **A use case without them becomes a sales story**
- **Do not mix it with a case study.** A use case is a way of using; a case study is a record. They need different sourcing
- Do not present a preview capability as an available way of using it

### Technical Paper Abstract (stage 3) (`whitepaper-abstract`)

![Technical Paper Abstract (stage 3)](images/slide-templates/whitepaper-abstract.png)

The cover page that lets a reader judge, before reading, what the paper settles

**Answers**: What is in this paper, and what will I know after reading it?

**figures**: `governing_message`, `lead_in`, `flow`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `toc` | `array` | ✔ | 3–5 items |
| `answers_q` | `string` | ✔ | ≤ 100 chars |
| `who` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **For a general audience.** Nothing from which a particular company or person can be inferred
- **Say what the reader will know afterwards.** A cover with only a table of contents gives nothing to decide on
- **Name the intended reader.** Stage 3 is the technical-proof stage, and the reader is an evaluator
- No unverified claim in the abstract. Hypotheses are handled as hypotheses in the body
- Performance figures, prices and editions fall under the 3-month rule. Put the last-confirmed date in source

<a id="partner"></a>

## Partner Pack (`partner`)

The two partner-facing materials the playbook defines but had no template for: what a partner gains by carrying the product, and the joint-proposal policy that settles RACI, the route to market, the estimate boundary and where responsibility sits.

### Partner Value and Shape of the Collaboration (`partner-value`)

![Partner Value and Shape of the Collaboration](images/slide-templates/partner-value.png)

What a partner gains and what they carry — a page to show a partner

**Answers**: What does a partner get out of carrying this product?

**figures**: `governing_message`, `lead_in`, `cards`, `so_what`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `items` | `array` (string≤16, string≤60) | ✔ | 3 items |
| `ask` | `string` | ✔ | ≤ 100 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Partner-facing.** Keep customer-specific confidential detail to a minimum (playbook §3)
- **Write it as standard, reusable information.** Mixing in one deal's circumstances makes it unusable elsewhere
- State what is in it for the partner (deal size, services, recurring work). **Not only what suits us**
- Do not name a competitor's weakness. Partners trade with the competition too
- Treat prices as list price (excl. tax) reference-estimate material; discounts and multi-year terms stay off (they route to the AE)

### Joint Proposal Policy (`joint-proposal-policy`)

![Joint Proposal Policy](images/slide-templates/joint-proposal-policy.png)

Settles the joint win theme, RACI, route to market, estimate boundary, IP and confidentiality, and contractual responsibility on one page

**Answers**: Who is responsible for what on this deal, and where does the estimate divide?

**figures**: `governing_message`, `lead_in`, `table`, `source_note`  
**Inference level**: Descriptive (organizing facts)  
**status**: experimental

**Inputs**

| Slot | Type | Required | Constraints |
|---|---|---|---|
| `title` | `string` | ✔ | ≤ 70 chars |
| `lead` | `string` | ✔ | ≤ 100 chars |
| `rows` | `string[][]` | ✔ | 5–7 items; ≤ 50 chars |
| `source` | `string` | ✔ | ≤ 200 chars |

Guardrails:

- **Partner proposal material.** Do not present an unagreed item as agreed
- **Always state the estimate boundary.** Left vague until it reaches the customer, it will be disputed later
- Do not omit where IP, confidentiality and contractual responsibility sit (required by playbook §3)
- An unagreed item stays as a row marked 未合意. **Do not ship it blank**
- Before naming the customer or their terms, confirm the NDA is in place
